# binaries / build
*.exe
*.test
/bin/
/dist/
# stray `go build` outputs at the repo root (build to a scratch dir with `-o`)
/seed-dev
/backend/seed-dev
# env & secrets — never commit
.env
.env.*
*.pem
*.key
# node
node_modules/
.next/
# editor/os
.DS_Store
Thumbs.db
# local scratch
/tmp/
coverage.out
