# Reviewer Gates — Telco Digital Credit Platform

**Role:** Lead reviewer (appointed 17 Jul 2026). Continuous review at milestone/commit boundaries against SRS v3.0 Volumes 1–3, ADR-0001 and BUILD_PLAN.md. The builder agent implements; nothing merges to a milestone "done" state without a gate review recorded in `build/reviews/`.
**Method:** verify at source, not by report — read the decision function, run the tests, attempt the negative case. Red is information; tests are never weakened to pass a gate.

---

## Standing findings (open until closed — apply from M0)

| ID | Severity | Finding | Required action | Status |
|---|---|---|---|---|
| SF-1 | HIGH (process) | Requirement-ID collisions across volumes (V3 review D-1): `CFG-001`, `COL-001`, `REG-001`, `TEN-001`, `SEC-001`… are defined with different text in Vol 1, Vol 2 and Vol 3. BUILD_PLAN itself cites bare IDs. The moment tests/evidence name requirement IDs (M0 CI), traceability is ambiguous. | Adopt `V1-`/`V2-`/`V3-` prefixes in ALL code comments, test names, evidence and reviews from the first commit. EDG-, INV-, DD- and SIM- IDs are unique already and stay bare. | FOLDED-Rev2 — verify in CI test names at G0 |
| SF-2 | MEDIUM (design tension — record, don't change) | `advances` partial unique index on open states hard-codes max-one-active-advance in the schema, while V1-PRD-005 / ADV-008 and V2-COL-008 make concurrency **configurable per programme**. | Acceptable and preferred for R1 (DD-09 default = 1; DB backstop beats app logic). Record in an ADR note: enabling N>1 for a programme is a **schema-level change gated by architecture review** (drop/replace index + tested recovery waterfall), not a config flip. Config validation must REJECT `max_concurrent_advances > 1` while the index exists — config that silently cannot take effect is the Nirvet anti-pattern (armed-but-dead control). | FOLDED-Rev2 (ADR amendment + plan §3) — verify config-validation code at G0/G2 |
| SF-3 | MEDIUM (test gap) | BUILD_PLAN §7 testing plan omits **tenant-isolation negative tests** (V2-TST-005 / SEC-016): attempted cross-tenant reads, writes, cache access, event consumption and export must be exercised adversarially in CI, not assumed from RLS presence. | Add a `tenantisolation` test pack to M0 exit criteria (wrong-credential + conflicting payload telco_id per V2-TEN-003 / EDG-026; RLS bypass attempt via each repo method). Runs in CI with `-race` from M0 onward. | FOLDED-Rev2 (plan §7 + M0 exit) — verify pack green at G0 |
| SF-4 | LOW-MED (document the stance) | Postgres outbox dispatched with `FOR UPDATE SKIP LOCKED` does **not** preserve per-aggregate ordering (V2-EVT-004 promises ordering within partition key). | Two acceptable resolutions — pick one and write it into ADR-0001 consequences: (a) dispatcher serialises per `aggregate_id` (claim groups by aggregate, ordered by ULID); or (b) declare ordering NOT guaranteed and make every consumer sequence-/pending-match-tolerant (V2-EVT-005), with reversal-before-original as the proof case. Silent middle ground is not acceptable. | RESOLVED-Rev2: option (a) per-aggregate FIFO chosen + consumers stay tolerant; supporting index added — verify dispatcher SQL at G0 |
| SF-5 | LOW (config) | `idempotency_records` TTL sweep: V2-API-003 requires replay to return the original outcome for the legitimate retry window. | TTL must be a config record (per BUILD_PLAN §8) with a seeded default comfortably ≥ the longest channel retry window (USSD gateway + SMS fallback + support-initiated re-query), and the sweep must never delete records for advances still in non-terminal states. | FOLDED-Rev2 (plan §3 idempotency_records row) — verify at G1 |
| SF-6 | MEDIUM (process) | Gating decisions DD-01 (legal entity), DD-02 (first telco), DD-03 (funding model), DD-05 (USSD route), DD-14 (post-20-Jul regulatory baseline) are unresolved. V1-GOV-007 / V3-V3A-004 prohibit silent defaults. | Create `build/ASSUMPTIONS.md`: every DD the build implicitly answers (e.g. "simulator profiled on MTN-shaped contracts", "own-balance-sheet funding pool seeded") recorded as a time-bound assumption with a review trigger (DD-14 trigger = the 20 Jul 2026 DEON ruling). | CLOSED-Rev2: ASSUMPTIONS.md A-1..A-14 created; live doc re-reviewed at every gate; A-5 trigger = 20 Jul DEON ruling |
| SF-7 | SHOULD (structural preference) | Ledger balance (V2-LED-001) enforced by app-layer assertion inside the posting txn + nightly rebuild. Good, but the strongest form is structural: a deferred constraint trigger asserting Σdebit=Σcredit per (journal, currency) at COMMIT. | Evaluate in M1; if the trigger costs <~10% posting throughput, adopt it. App assertion remains either way. | SCHEDULED: prototype-and-measure at M1 (plan §7) |

## Milestone gates (exit review before a milestone is called done)

Each gate = builder self-evidence + my independent verification. Evidence lands in `build/reviews/REVIEW-<n>-M<k>.md`.

### G0 — Foundation (BUILD_PLAN M0)
- Fresh-DB migration from zero in CI (from-zero lesson); FORCE-RLS-aware runner proven with a seeded global row.
- Tenant middleware: wrong-tenant credential and conflicting payload `telco_id` rejected + security-audited (V2-TEN-002/003, EDG-026). **SF-3 pack green.**
- Config service: version immutability after approval, maker≠checker enforced at API level, decision pins version set (V1-CFG-002/003/007).
- Idempotency store: crash-after-commit replay returns original response (V2-API-003).
- `go test -race ./...` green; CI blocks merge on failure (V2-TST-016 wiring starts here).
- **SF-1 resolved before this gate closes** (prefix convention in place).

### G1 — Walking Skeleton (M1, first push to main)
- End-to-end demo vs simulator: offer → confirm (idempotent) → reserve → fulfil → recover → balanced ledger → reconciliation (V3-DLV-003).
- I independently run: EDG-001 (duplicate confirm), EDG-002 (concurrent requests — DB backstop proven by disabling the app lock in a test build), EDG-004, EDG-005 (timeout-after-success — THE case), EDG-007 (crash after telco success), EDG-009, EDG-018.
- Ledger rebuild equals stored balances (V2-LED-008); no journal for PENDING/UNKNOWN fulfilment (V2-LED-006).
- Reservation release exactly-once on failure (V2-ADV-010); no txn held across the simulator call (V2-ADV-006) — verified by reading the saga code, not the PR description.
- SF-4 stance written and demonstrated (reversal-before-original parked and resolved).

### G2 — Credit Core (M2)
- Anti-gaming: ₦20k-spike-on-₦2k-baseline scenario produces capped contribution + ≤1 tier movement + retained anomaly feature (V1-CRD-004/005/006, EDG-013).
- Stale/corrupt feed → quarantine, no partial silent update (EDG-014); staleness policy fail-closed default (V2-SCR-016).
- Decision replay reproduces original result from pinned versions (V1-CRD-010, V2-SCR-011).
- Real-time overlays actually suppress: SIM-swap flag mid-day kills the precomputed offer (EDG-012) — I test the overlay path, not the batch path.
- Disclosure/consent: content hash retained per advance; no pre-ticked/auto acceptance possible (V1-REG-002, V2-REG-001).
- 1M-synthetic-subscriber scoring run inside window; hot-path offer read = point read (V2-TAR-004) with EXPLAIN evidence.

### G3 — Money Core (M3)
- Full recovery matrix: partial, over-recovery capped + suspense (EDG-020), reversal-before-original (EDG-019), post-write-off (EDG-021), duplicate events (EDG-018 at volume).
- Guardrails: simulated mass over-approval trips suspension within threshold; re-arm requires maker-checker + incident record (V1-CRD-013/014, EDG-024). Verify the guardrail has a **zero-config floor** — empty/missing threshold config must fail closed, never disarm (reachability-invariant lesson).
- Funding: reservation storm vs pool cap — `CHECK (reserved + utilised <= committed)` holds under `-race` load; exhaustion mid-request = safe decline (EDG-023).
- Settlement statement reproducible from ledger to the kobo (golden recon test); breaks preserved, no force-match (EDG-027, V1-FIN-005).
- Bureau export dormant-capable: produces validated file + reconciles to eligible population without submitting (V1-REG-006/007).

### G4 — Portals (M4)
- Server-side authz: every portal query tenant-scoped at the API, UI hiding insufficient (V2-UI-001) — I attempt cross-tenant and cross-role fetches directly against the BFF.
- Maker-checker unbypassable in UI AND API (V2-UI-003); MSISDN masked by default with audited reveal (V2-UI-004).
- Raw operator/audit data surfaces = platform-admin scope only (Nirvet audit-authz lesson).
- Manual repair tooling creates events + ledger corrections, never state edits (V1-ADV-012, V3-AFO-011).

### G5 — Hardening / Certification-Ready (M5)
- EDG-001..040 coverage matrix 100% (each maps to a named test or an approved N/A with reason).
- Load: USSD peak + recharge burst + batch scoring concurrently; no acknowledged event loss (V3-SVC-009); no cross-tenant starvation (V2-TEN-009).
- Chaos: worker kill mid-saga, DB failover locally — no duplicate economic effect (V2-RES-005/006/007).
- Security pass: authz matrix tests, secret scan, log-masking scan (no full MSISDN/NIN in logs, V2-SEC-008).
- R1 Proportionality Annex drafted (V3 review strategic caution) for owner approval.

## Review rules of engagement
1. Builder notifies at each milestone candidate; I review from source at that boundary. Interim commits reviewable on request.
2. Findings are numbered (SF- standing, G<k>-F<n> gate findings) with severity; CRITICAL/HIGH block the gate, MEDIUM needs owner-visible disposition, LOW is tracked.
3. I never weaken a failing test to pass a gate; if a test is wrong, the fix is a reviewed change with rationale.
4. Any deviation from BUILD_PLAN §3–6 binding patterns (schema keys, idempotency matrix, concurrency patterns, SQL-in-repo rule) is an architecture defect (V2-ARC-007), not a style comment.
5. Requirement citations in reviews use volume prefixes (SF-1).

---

## Verification log

### VR-1 — 17 Jul 2026: SF disposition verified at source (pre-start)
Reviewed BUILD_PLAN Rev 2, ADR-0001 (with amendments), ASSUMPTIONS.md line-by-line + mechanical grep for unprefixed citations across build/.

**Result: all seven SF conditions genuinely folded in — cleared to start M0.**
- SF-1: zero unprefixed requirement citations remain in BUILD_PLAN Rev 2; the V1-TRE-010 vs V2-TRE-010 miscite correction is substantively right. Convention text + CI test-name format in ADR amendment. REVIEW_GATES.md itself normalized to the hyphenated format (reviewer holds own docs to the same rule).
- SF-2: closed as specified — config validation rejects N>1 with explanatory error while index exists; N>1 is architecture-gated schema change (ADR amendment + plan §3 advances row).
- SF-3: tenant-isolation negative pack in plan §7 + M0 exit criteria, adversarial per-repo-method, in CI with -race.
- SF-4: option (a) per-aggregate FIFO with a correct, conservatively race-safe claim predicate (a locked-but-uncommitted older row still blocks newer rows for the same aggregate); supporting partial index `(aggregate_id, id) WHERE published_at IS NULL` added; consumers remain pending-match tolerant because telco-INBOUND events have no ordering guarantee regardless — right reasoning.
- SF-5: TTL as config with seeded floor; sweep never deletes records for non-terminal advances (plan §3).
- SF-6: ASSUMPTIONS.md A-1..A-14 created; A-5 conservative-regulatory-superset is the correct posture ahead of the 20 Jul DEON ruling; expired-untouched assumptions are gate-blocking per its own standing rule.
- SF-7: prototype-and-measure scheduled at M1 (plan §7), <~10% threshold, app assertion retained either way.

**Residual LOW nits (no gate impact, fix opportunistically):**
- VR-1a: ADR-0001 Context line still cites bare `ARC-002` (should be V2-ARC-002). Prose-only; ARC exists only in V2 so unambiguous, but the convention is binding.
- VR-1b: BUILD_PLAN §10.3 says local Postgres "port TBD" while ASSUMPTIONS A-14 decides 5434 — align §10.3 to A-14.

**Next checkpoint: G0** (foundation exit). Builder notifies at M0 candidate; reviewer verifies fresh-DB migration, tenant middleware negatives, config maker-checker, idempotency crash-replay, SF-1 CI naming, SF-3 pack, SF-4 dispatcher SQL — at source.

### VR-2 — 17 Jul 2026: G0 gate review (M0 commits 0889b82 + 86843d5)
Full source read + independent `-race` run in golang:1.25 vs telco-credit-postgres. Record: `build/reviews/REVIEW-1-M0.md`.
**Verdict: G0 CONDITIONAL PASS.** Builder suite all green; SF-1..6 verified implemented at source (not just referenced); suspected audit-RLS-rejection cleared (NULLIF guards it, test proves the row lands).
- **G0-F2 (HIGH, must-fix before G1):** outbox head-of-line starvation. Unhandled-type or max-attempts-parked events re-fill the claim batch every cycle; 50 poison events on distinct aggregates stall all dispatch. Reviewer reproducer `outboxdispatch/reviewer_starvation_test.go` FAILS (delivered=0/10 cycles). Confirmed real. Blast radius opens at M1 (ledger/recovery/fulfilment events ride this backbone; V2-EVT-012). Fix = filter claim by registered event types + dead-letter at max-attempts; keep per-aggregate FIFO guard. G1 gated on this + reproducer green.
- **G0-F3 (LOW):** VR-1a bare ARC-002 in ADR, VR-1b plan §10.3 "port TBD" — still open; opportunistic.

### VR-3 — 17 Jul 2026: Elevated to best-in-class standard (owner directive)
Owner directive: build correctly + best-in-class + better than competition, from the ground up. Created **build/ENGINEERING_STANDARD.md** (BC-1..8) — binding at ADR authority, enforced at every gate alongside SRS MUSTs + SF findings. Selection rule = retrofit-expensive × competitively-differentiating. Two urgent foundational gaps confirmed at source (both pre-M1):
- **BC-GAP-1 (HIGH, pre-M1):** no typed `Money` value object — money is bare `int64 + currency string`. No floats yet (good) but nothing enforces it. Must land `Money` type + rounding-policy ADR as the FIRST M1 primitive, before the saga/ledger/recovery/settlement pass raw int64 (retrofit = multi-week rewrite across the money path). Compile-time currency safety + single rounding site + CI float-ban = the ledger differentiator.
- **BC-GAP-2 (HIGH, immediate/G0-amend):** CI has vet/build/migrate/test but NO golangci-lint, govulncheck, gosec, gofmt-check, go mod verify, or coverage floor. Clean static/security baseline is free at ~30 files, unachievable at 3000. Add the quality+security merge-blocking stage NOW; commit .golangci.yml; drive tree to zero warnings (trivial at current size).
Gate criteria upgraded: G0 now also requires BC-2; G1 requires BC-1/BC-3/BC-6/BC-7; G2 requires BC-4; BC-5/BC-8 continuous. A BC regression is a gate finding at the severity of what it protects.

### VR-4 — 17 Jul 2026: Pre-M1 batch verified — G1 ENTRY CLEARED (commits 9fc85ac, 852c4c0, e4e4bb6, 1f159f4)
Verified at source + independent -race run (golang:1.25 container). **All G1-entry conditions met; builder cleared to proceed to M1b (walking-skeleton saga).**
- **G0-F2 CLOSED:** migration 0002 (dead_lettered_at + claim-window index excludes DLQ; FIFO-guard index deliberately unchanged), type-filtered ClaimBatch (`event_type = ANY($2)`, empty registry claims nothing), dead-letter at max-attempts, operator Requeue (V2-EVT-009), DeadLetteredCount metric. Critical subtlety implemented exactly as prescribed: inner NOT-EXISTS ignores BOTH type filter and DLQ marker — quarantined head still blocks its own aggregate, never skips. **Reviewer reproducer now GREEN in my own run**; builder's deadletter_test proves block-own-aggregate + flow-other-aggregates + requeue-unblocks + empty-registry-untouched.
- **BC-GAP-1 CLOSED (BC-1):** entity.Money + ADR-0002. Unexported fields (zero value unusable, errors on all ops), overflow-checked arithmetic incl. MinInt64/-1 pre-check, PercentBps = THE single rounding site (half-up away from zero, big.Int intermediates), AllocateByRatio = largest-remainder, Σparts==total exact, deterministic tie-break; JSON round-trips through the constructor. 10k-case property test. LOW polish note: AllocateByRatio q.Int64() lacks IsInt64 guard for the |MinInt64| single-ratio nano-edge (lands correct by two's-complement accident) — add guard opportunistically.
- **BC-GAP-2 CLOSED (BC-2):** CI quality job merge-blocking: gofmt, mod verify+tidy-diff, money-path float-ban grep, golangci-lint (zero warnings; documented pgx-Rollback exclusion), govulncheck (clean after toolchain 1.25.12 bump — 19 stdlib CVEs were toolchain not code), gosec (clean; 2 auditable #nosec), coverage floor 70% (at 73.9%). 
- **G0-F3 CLOSED:** VR-1a (V2-ARC-002 prefixed) + VR-1b (port 5434 aligned) both fixed.
- **M1a (beyond plan, owner no-hardcoding directive):** admin config API (draft→submit→approve→activate over HTTP, actor from credential, BC-7 typed errors mapped once: 409/422/404), migration 0003 (admin_credentials hash-only + 6 seeded M1 domains), validators_m1.go — armed-but-dead prevention with teeth: telco.adapter retry_budget FORCED 0 citing INV-009 (admin cannot configure the blind-retry double-credit vector), recon.tolerance seeded ZERO w/ auto-resolve off (fail-closed floor), auto_resolve+zero-tolerance rejected as no-op-that-reads-as-control. OpenAPI 0.1.0 same-commit; spec-drift test updated.
**Next checkpoint: G1** (M1b walking skeleton, first push to main). I will independently run EDG-001/002/004/005/007/009/018, read the saga for no-txn-across-network-call (V2-ADV-006), verify BC-3 (property-based invariant checker), BC-6 (correlation edge→journal), SF-7 trigger measurement, and LED-006 (no journal for PENDING/UNKNOWN).

### VR-5 — 17 Jul 2026: Interim review of M1b-1 (f214668) — credit-core schema + sole-writer ledger
Commit-boundary review (not a gate): last cheap moment to adjust ledger/schema shape before the saga consumes it. Record: `build/reviews/REVIEW-2-M1b1-interim.md`. Independent -race run green (incl. new ledger + dbmigrate packages).
Verified: append-only-by-grants (test proves DB refusal), balance-before-write per currency, posting idempotency w/ original-id return, mandatory correlation_id (BC-6), fail-closed governed chart of accounts, funding CHECK, load-bearing advances_one_active_uq name, transitive RLS via EXISTS-on-parent, Vol-2 FSM (delinquency=overlay). Build-cache incident response exemplary (distrust-host-builds + migration-contract regression test).
**Findings (fold into next commit, none block M1b-2/3):** M1B-F1 MED dangling "A-15" reference — the single-role ledger-write deviation from V2-LED-015 is deliberate but NOT in ASSUMPTIONS.md; add the row. M1B-F2 MED ledger swallows divergent duplicates — same business_event_key with DIFFERENT lines returns original silently; add lines-hash + loud ErrDivergentDuplicate. M1B-F3 LOW-MED offer money-identity CHECK only covers DEDUCTED_UPFRONT; make exhaustive per model. M1B-F4 LOW advances.offer_id lacks UNIQUE (offer-accepted-once is app-side only). Reminders to G1: SF-7 measurement due; posting-template-engine M3 deferral should be recorded in plan §9.

### VR-6 — 17 Jul 2026: M1B-F1..F4 fold verified + M1b-2 adapter review (68d351e, 55f3265)
Independent -race run green (incl. new mno package). **All four interim findings verified genuinely closed:**
- M1B-F1 ✓ A-15 row is thorough (deviation, why-safe, trigger = tcp_ledger role at post-M3 service split, impact).
- M1B-F2 ✓ lines_hash = sorted-concatenation sha256 (NOT xor — collision-safe), unambiguous field encoding (`Account|Side|Currency|Amount` joined by \n); divergent duplicate → typed ErrDivergentDuplicate, writes nothing; three-path test (identical / reordered-identical / drifted).
- M1B-F3 ✓ migration 0005 replaces the CHECK, exhaustive over both fee models incl. repayment pinning. Correct discipline: new migration, not an edit to applied 0004.
- M1B-F4 ✓ advances_offer_uq.
**M1b-2 adapter verified:** canonical Client interface (saga depends on interface, not HTTP); INV-009 structural (no retry code path exists); conservative classification per V2 Appendix D — transport error/5xx/200-with-garbage/unrecognised status → Unknown (V2-TEL-009 quarantine); enquiry read-only and safely repeatable; OutcomeNotFound = provably-never-landed → safe fail; endpoint+timeout from telco.adapter config at call time (proven by re-point-via-admin-API test); raw wire evidence retained both directions (V2-TEL-002); EDG-005 timeout-after-success primitive green at the adapter layer.
**Two new findings (LOW tier, fold opportunistically before G1):**
- M1B2-F1 (LOW): `ledger.accounts` config domain has NO registered validator — an empty/malformed chart is approvable. Posting fails closed (safe direction) but it's an admin foot-gun outage vector; add validator: non-empty accounts, code pattern ^[A-Z][A-Z0-9_]+$ (also removes the hash-encoding nano-ambiguity).
- M1B2-F2 (LOW-MED): SubmitFulfilment classifies ALL 4xx as Failed. 408 (edge/aggregator timed out) and 429 can arrive while the telco backend still processes — the exact shape INV-009 guards. Narrow Failed to an explicit definitive-rejection allowlist (400,401,403,404,409,422); everything else non-2xx → Unknown. Cheap, closes an edge double-credit vector at aggregator-fronted telcos.
Builder proceeds to M1b-3 (origination saga) as planned; fold the two LOWs with it.

### VR-7 — 17 Jul 2026: M1b-3 saga core review (e69b26e + 1aa0a34)
Independent -race run green (incl. new origination package). Read the full saga source.
**Verified at source:**
- VR-6 LOWs closed: definitive-rejection allowlist (table test proves 408/429/502/503 → Unknown); ledger.accounts validator (non-empty, code pattern) registered.
- **No-txn-across-network-call is structural** (V2-ADV-006): tx1 accept→advance→reserve→attempt→PENDING commits BEFORE the adapter call; tx2 resolves. Crash window between tx1/tx2 documented and safe: SENT attempt on PENDING advance → resolver treats stale SENT as UNKNOWN → enquiry (EDG-007 exactly-once recovery).
- **Deadlock fix is structurally sound** (mig 0006): advance born pool-less so the one-active contest is decided at the INSERT before any pool lock — a losing contender never holds the pool row; `advances_pool_by_state` CHECK guarantees pool NOT NULL from EXPOSURE_RESERVED onward. The builder diagnosed from live Postgres deadlock DETAIL, not guesswork, and the EDG-002 test (8 concurrent confirms, exactly-one-success + deterministic block classes) caught both first-pass bugs before I saw them — BC-8 working as designed.
- **Ledger algebra traced for BOTH fee models**: DEDUCTED_UPFRONT Dr receivable(face)=Cr clearing(face-fee)+Cr fee(fee); ADDED_TO_REPAYMENT Dr receivable(face+fee)=Cr clearing(face)+Cr fee(fee). Balanced. Recognition at confirmed fulfilment only (V2-LED-006/A-10); UNKNOWN = no journal, no utilisation, reservation held.
- ResolveOutcome shared saga/resolver (semantics cannot drift); posting idempotency makes the dual path safe; OutcomeNotFound = provably-never-landed → safe fail; enquiry schedule from governed config whose validator enforces non-empty positive non-decreasing delays (no [0] panic via config).
- Offer double-accept blocked twice: FOR UPDATE + from-state guard, advances_offer_uq backstop. Exposure convention: reserve/utilise = repayment obligation (outstanding) — consistent everywhere and matches V1 §14 exposure language; conservative direction.
**Findings (all LOW, fold with M1b-4/5, none gate-relevant):**
- M1B3-F1 (LOW): GetOffers ladder generation is not concurrency-idempotent — two simultaneous first-time GetOffers both insert ladders → duplicate valid offers. Economics protected (one-active + offer-uq) but violates V2-OFR-009 SHOULD and doubles USSD menu entries. Fix: advisory xact lock on (subscriber, programme) around generate-if-absent, or unique (subscriber_account_id, programme_id, face_value_minor) WHERE state='GENERATED'.
- M1B3-F2 (LOW): repeated UNKNOWN enquiry cycles emit a fresh advance.FulfilmentUnknown outbox event per cycle — event noise; consider emitting only on state ENTRY (PENDING→UNKNOWN), with enquiry attempts visible via attempts table instead.
- M1B3-F3 (NIT): buildLadder comment says "skip loudly in caller logs" for fee-consumes-denomination but no log call exists — log it or drop the comment.
Remaining before G1: channel HTTP API (M1b-3 tail), recovery+resolver (M1b-4), recon + BC-3 checker + SF-7 measurement + E2E (M1b-5).

### VR-8 — 17 Jul 2026: M1b-4 recovery engine + resolver review (b11b235)
Independent -race run: all 11 packages green (first attempt hit transient container network loss mid-module-download — resolved with persistent gomodcache volume `tcp-gomodcache`; not a code issue).
**Verified at source:**
- **The critical recovery-safety property holds:** `FindOpenBySubscriber` filters `state IN ('ACTIVE','PARTIALLY_RECOVERED')` ONLY — garnishment can never recover against PENDING/UNKNOWN (value the customer may never have received). Locked FOR UPDATE, oldest-first (V2-COL-002).
- Recovery Ingest = one tx end-to-end (no network inside): DB-arbitered source-event dedup (replay returns original, touches nothing — EDG-018); applied-vs-excess split exact; waterfall config-driven with recovered-so-far accounting, components provably sum to outstanding (consume-fully guard is a real invariant); pool utilisation reduced by applied (V2-TRE-004); applied + suspense journals both balanced; over-recovery = explicit RECOVERY_SUSPENSE liability (EDG-020); unmatched events preserved as UNMATCHED, never discarded.
- **Builder self-caught a no-hardcoding violation** (quarantine path nearly booked programme-less events to a hardcoded programme) and resolved it correctly: suspense record now, ledger attribution honestly deferred to M3/DD-19 with a proper §9 register entry (verified present, line 130). The discipline is self-enforcing now.
- Resolver claims due UNKNOWN + stale-SENT (the tx1/tx2 crash window), enquires OUTSIDE any tx, applies via the SAME origination.ResolveOutcome — drift impossible. EDG-005 continuation, EDG-007 (CreditDirect fault injection — credited-but-never-heard recovered exactly once), EDG-008 (NOT_FOUND → FAILED + release) all covered.
- VR-7 LOWs closed: per-(subscriber,programme) advisory lock (6-racer test → one ladder); FulfilmentUnknown event on state ENTRY only (hanging-enquiry cycle test → zero new events); comment fixed.
**One NIT (M1B4-F1):** `allocate()` does raw-minor arithmetic (`compTotal.Amount() - compRecovered`, `MustMoney(recovered[a]+recovered[b])`) because `SumByComponent` returns `map[component]int64` — a small BC-1 deviation in the usecase layer. Have the repo return Money per component; fold whenever convenient.
**Remaining to G1:** HTTP surface (channel + recovery endpoints w/ OpenAPI), M1b-5 (recon vs simulator log, BC-3 property-based invariant checker, SF-7 measurement, wired E2E demo). G1 = full gate + first push.

### VR-9 — 17 Jul 2026: VR-8 NIT fold verified (60fe1ac)
Spot-check at source + targeted -race run (recovery + repo green). `SumByComponent` returns `map[component]entity.Money` built through the validated constructor with currency carried from the DB row — a cross-currency component now surfaces as a loud integrity error, and the waterfall runs entirely in Money operations. Zero bare-integer money above the SQL scan line. **Open findings: none.** Next review = G1 full gate (complete EDG pack re-run, BC-3 checker verification, SF-7 measurement check, saga/HTTP/recon sweep) at walking-skeleton completion.

### VR-10 — 17 Jul 2026: M1b-3 HTTP surface review (0df03ca)
Independent -race run: all 12 packages green. Read channel.go in full.
**Verified at source:** Correlation middleware mints-or-accepts + echoes + context-propagates (BC-6); wire test proves the customer's exact correlation id lands on the journal. BC-7 mapping lives in exactly one function (writeDomainErr); unmapped errors render opaque 500 with detail logged, never leaked (V2-API-011). Idempotency-Key validated 8..128 BEFORE the domain (V2-API-002); bodies bounded (MaxBytesReader 64KB). Customer-safe vocabulary correct: all pre-terminal/ambiguous states → PROCESSING with durable status_route; 201 ACTIVE / 200 replay / 202 UNKNOWN / 422 definitive-fail (V2-ADV-016, EDG-001/004 at wire level). Layering held (GetAdvance usecase accessor; handlers never touch repos). Cross-tenant advance lookups invisible via RLS. OpenAPI 0.1.1 same-commit, drift test updated.
**Two findings (LOW/NIT — fold with M1b-5):**
- VR10-F1 (LOW): caller-supplied X-Correlation-Id is unbounded/unvalidated — up to Go's ~1MB header limit could persist into IMMUTABLE journal rows and logs. Bound it (e.g. ≤64 chars, [A-Za-z0-9_-]); re-mint when invalid (still echo the minted one).
- VR10-F2 (NIT): GET /v1/advances/{id} 404 renders family OFFER_NOT_FOUND — misleading for the status route; render ADVANCE_NOT_FOUND (spec + drift test in same commit per §5a).
Next: G1 full gate at M1b-5 completion.

### VR-11 — 17 Jul 2026: **G1 FULL GATE — PASS** (3bcbd6c). First push to main AUTHORIZED.
Record: `build/reviews/REVIEW-3-G1.md`. Everything run/read by the reviewer directly: full 13-package -race suite green; named EDG pack (001/002/005×3/007/008/011/018/020 + G0-F2 reproducer) all PASS in reviewer's hands; wire-level walking skeleton PASS; BC-3 checker verified (11 set-based sweeps incl. ledger-vs-book + pool-vs-book cross-checks) and operator job run by reviewer → exit 0 "all invariants hold"; randomized-histories property test verified genuinely randomized (deterministic seed = right trade-off); SF-7 honesty check PASSED (+31.8% vs <10% bar → correctly declined, backstop proven firing, M3 revisit recorded); VR-10 folds verified in code. Zero open findings. Carried notes: G1-N1 NIT add INV-018 one-active sweep; G1-N2 LOW M5 randomized-seed soak variant; G1-N3 pre-production role-credential rotation (devlocal_* never reach prod). A-5/DD-14: DEON ruling 20 Jul — trigger not yet fired, re-review mandatory when it lands; conservative superset means no outcome changes the skeleton. Owner decision needed: git remote (recommend private GitHub/ArowuTest). Next: G2 credit core.

### VR-12 — 18 Jul 2026: **G2 FULL GATE — CONDITIONAL PASS** (M2 series through 1e5ffa9). Record: build/reviews/REVIEW-4-G2.md.
Independent 17-package -race suite green; named M2 tests (EDG-013 wash/spike, EDG-014 staleness, SCR-007 one-tier, BC-4 bit-exact + tamper, overlay-blocks-offer-and-confirm, consent-in-confirm-tx) all PASS in reviewer's hands. Repo confirmed PRIVATE (unauth 404); deployment commits reviewed post-hoc — G1-N3 properly closed (env-rotated role passwords), managed-cluster fallback CI-proven, gosec annotations audited genuine. Scale proof honest (89→2,690 subj/s via set-based staged-COPY; 1M in 15.8min).
**Three MEDIUM findings — fold + reviewer verification REQUIRED before M3 begins:**
- **G2-F1:** `missing_policy` validated (REJECT|STARTER, "silent imputation forbidden") but consumed by NOTHING — armed-but-dead inside the validator layer itself. Implement or delete; validated-and-ignored may not survive.
- **G2-F2:** `SPIKE_DISCOUNT_APPLIED` reason code asserts an un-taken action — spiky's only effect is the label; winsorisation runs identically either way. Rename to SPIKE_ANOMALY_DETECTED + make the spiky-consequence an explicit recorded policy decision (may be "flag only", but decided, not accidental). Regulator-facing explainability (V1-CRD-008).
- **G2-F3:** validateRow has NO upper bound on weekly recharge — corrupt near-int64-max row passes validation, overflows maxW*10_000 to negative (spike check silently bypassed), garbage tier assigned. EDG-014 violation at the boundary. Fix: config plausibility ceiling at ingest (quarantine above), + optional overflow-safe engine arithmetic.
Carried: G2-N1 unchecked engine ints (M5); G2-N2 worker-as-owner demo-only; G1-N1/N2 open; branch protection unverified (builder to confirm). Winsor-disarm fix verified GENUINE (validator floor 9230 w/ rationale, mig 0008, pinned test). Next: G2-F fold verification → G3 money core. DEON ruling Jul 20 → A-5 re-review.

### VR-13 — 18 Jul 2026: G2 findings verified CLOSED (b9605b7) — **M3 CLEARED**
Independent 17-package -race suite green; fold read at source.
- **G2-F1 ✓** MISSING_FIELDS quality flag routes through explicit policy: REJECT → MISSING_DATA_REJECTED ineligible; STARTER → progressive-trust floor; an unvalidated value reaching the engine ERRORS ("not implementable"), never defaults. Simulator emits the flag on a deterministic profile so real files exercise the path.
- **G2-F2 ✓** Reason code now factual (SPIKE_PATTERN_DETECTED); consequence is explicit validator-required config `anti_gaming.spike_action` with BOTH values implemented: FLAG_ONLY (winsorisation is the discount — recorded decision) and CAP_TO_STARTER (spiky cycle floored at starter, SPIKE_CAPPED_TO_STARTER). Unimplementable value = engine error. The no-code-claims-an-untaken-action property is test-asserted.
- **G2-F3 ✓** Plausibility ceiling from governed config read FAIL-CLOSED (absent ceiling = refuse the whole feed, never unlimited); rows above quarantined with reasons; simulator `?corrupt=1` near-int64-max row proven never to reach the feature store. Post-ceiling values (≤₦1m/week seeded) make the bps arithmetic overflow-impossible; engine-side guard retained as defense-in-depth.
- EngineVersion 1.2.0 with changelog discipline; migration 0010 supersedes deployed configs content-preservingly (dynamic SQL because environments carry admin-repointed versions — correct).
**Note (pre-production, EDG-039):** 1.1.0-era decisions now correctly report engine-version drift on replay rather than fabricating — the engine-artifact archive requirement (replay old decisions with their matching engine) must land before production; tracked with the go-live items.
**Builder cleared to begin M3 (money core).** Owner decision pending: branch protection (GitHub Pro vs public vs risk-accept) — reviewer recommends Pro.

### VR-14 — 18 Jul 2026: M3a interim review (6bfb562) — money-core schema + domains. CLEAN, zero findings.
Independent 17-package -race suite green; migration 0011 + validators_m3 read at source.
Verified: WRITTEN_OFF added to the state CHECK while the one-active partial index is deliberately NOT widened (crystallised loss ≠ lending block — correct); write_offs carry the distinct-approver CHECK at schema level (maker-checker structural, same pattern as config); settlement statements content-hashed for bit-identical regeneration with append-only lines; complaints require resolution before RESOLVED/REJECTED; pending_reversals PARKED/APPLIED/EXPIRED lifecycle; bureau staging dormant-by-design. Zero-config floors verified in validators: write-off `require_distinct_approver` must be true ("not configurable off — schema-enforced"); guardrail `rearm` must be MAKER_CHECKER ("two-person decision, not configurable off"); settlement telco+platform shares must equal exactly 10,000 bps; exposure guardrail bounded (0,10000]. Engine-artifact archiving logged in §9 register (git tags per EngineVersion interim; full requirement at M5 evidence pack). Builder continues M3b (recovery matrix: reversal-before-original application, post-write-off recoveries, DD-19 programme-attributed quarantine posting). Open on owner's desk: branch protection (Pro recommended), Render API key rotation. DEON ruling ~2 days.

### VR-15 — 18 Jul 2026: two owner risk-acceptances recorded (interim, expiry = go-live gate)
- **RA-1 Branch protection deferred:** owner accepts unprotected main until pre-go-live GitHub Pro upgrade. Compensating controls: single committer, push-only-at-gate-cleared-milestones discipline, CI runs on every push (detection). Expiry: GLV gate — protection REQUIRED before go-live (V2-DEV-001).
- **RA-2 Render API key 401 (rotation pending):** deploy path unaffected (GitHub webhook). Cost = programmatic observe/manage of the deployed environment (logs, deploy-health checks, env vars, remote operator jobs). Not urgent; REQUIRED before the G3 live-environment verification (reviewer will re-run the deployed money story incl. settlement) and before any incident-response reliance on the demo environment.

### VR-15a — 18 Jul 2026: RA-2 CLOSED — Render key verified working
Reviewer tested the key from the owner's local credential file ("The Cred.txt" in session outputs, NOT in any repo): HTTP 200 on /v1/services, BridgeXtra services visible (worker srv-d9d7q2qhil2s73ff13qg, auto-deploy from ArowuTest/BridgeXtra main). The builder's 401 was a stale key in their environment — builder to source the working key from the owner's credential file (via env var, never committed). G3 live-environment verification is unblocked.

### VR-16 — 18 Jul 2026: M3b interim review (a85058a) — recovery matrix. 1 MEDIUM finding.
Independent 17-package -race suite green; reversal/reopen/quarantine paths read at source.
**Verified good:** EDG-019 both directions (park idempotent per original; parked applies in same tx as original's allocation; pair nets to pre-pair book state — test-proven); over-reversal REFUSED loudly (surfaces in recon, never partially guessed); reversals vs unmatched/quarantined originals stay parked for the operator breaks workflow; EDG-021 post-write-off recovery → income, loss stays crystallised; DD-19 closed properly — `telcoLevelEvents` allowlist in the ledger (only RECOVERY_QUARANTINED may omit programme) with the 0012 schema CHECK as its structural twin; reopen posts the mirrored balanced journal and re-adds pool utilisation under the over-allocation CHECK.
**M3B-F1 (MEDIUM — fix within M3, before G3):** the reopen path (CLOSED → PARTIALLY_RECOVERED, recovery.go ~450) has NO handling for the one-active collision: advance A closed → subscriber legally opens advance B → reversal on A tries to reopen → `advances_one_active_uq` fires → raw unique-violation error aborts the tx. Money integrity holds (backstop), but the reversal is neither applied, parked, nor queued — telco retries fail forever, books silently diverge until recon flags it, and no operator queue entry exists. Same gap for the pool-headroom (ReAddUtilisation) failure the builder describes as "fails loudly" — loud to whom? Nothing records it. **Fix: catch BOTH collision classes and PARK the reversal with a distinct reason (e.g. REOPEN_BLOCKED_OPEN_ADVANCE / REOPEN_BLOCKED_POOL_HEADROOM) into the existing pending_reversals machinery + operator breaks queue; add the reopen-while-new-advance-open test (currently NO test covers it).** Design note: the one-active index intentionally counts a reopened advance — the rule protects new-lending concurrency and a parked reversal resolving after B closes is the honest sequence; do NOT widen the index.
Builder may continue M3c in parallel; M3B-F1 must close before G3.

### VR-17 — 18 Jul 2026: M3B-F1 verified CLOSED (b1b6edd) + M3c interim review (bed2825) — CLEAN
Independent 18-package -race suite green (new collections package included); fold + write-off paths read at source.
**M3B-F1 ✓ closed exactly per prescription, implemented precisely:** reversal application runs under a savepoint (pgx nested Begin); collision classification pinned to exact SQLSTATE + constraint name (23505/advances_one_active_uq → SUBSCRIBER_HAS_OPEN_ADVANCE, 23514/funding_no_overallocation → POOL_HEADROOM) — every other error propagates untouched, no over-broad catch; parked into pending_reversals with reason + freshest-reason on retry; the parked-then-original path is ALSO guarded so a collision can never sink the original's ingest (SetParkReason keeps operator visibility). Test walks my exact sequence incl. B-closes-then-retry-applies drain to APPLIED. One-active index untouched per prescription.
**M3c verified:** delinquency = set-based single-statement stamp from governed ladder, only-changed-rows (idempotent daily job); write-off maker-checker at schema + usecase, eligibility gated by min_bucket with CONFIG-DRIFT REFUSAL (min_bucket not on ladder = refuse, not guess — exactly the right instinct); loss itemised fee/principal reversal-aware; WRITE_OFF_EXPENSE journal posts in the same tx as crystallisation; EDG-021 re-proven through the real path post-write-off.
Zero new findings. Remaining M3 slices: M3d guardrails (EDG-024 surge sims), M3e posting-template engine + reproducible settlement, M3f breaks/complaints/bureau — then **G3 full gate** (incl. live deployed verification with the working Render key, zero-config-floor checks on every guardrail, settlement golden tests).

### VR-18 — 18 Jul 2026: M3d interim review (689a76d) — treasury guardrails. 1 MEDIUM finding.
Independent 18-package -race suite green; EDG-024/025 surge tests re-run by reviewer — both PASS. Guardrail architecture verified at source and CORRECT: EvaluateInTx measures inside the confirm tx AFTER the pool-row lock (the serialization point — concurrent confirms measure sequentially, the cap physically cannot be raced); a breach aborts the confirm while RecordTrip commits out-of-band with crash-window convergence (next confirm re-detects); suspended programme refuses at first touch (offers AND confirms); re-arm two-person at schema; recovery = governed limit raise, no deploy.
**M3D-F1 (MEDIUM — fix before G3): the guardrail is nil-skippable.** `origination.Service.Treasury` is a nil-able field guarded by `if s.Treasury != nil` at BOTH the evaluation and trip-recording sites, with the comment "Nil only in tests that predate guardrails — production wiring always sets it." This is the reachability-invariant violation in its purest form: a safety control that can be structurally absent with no error, no log, no refusal. Today's wiring is safe (New() always sets it) — but nothing ENFORCES it: any future struct-literal construction (new cmd, copied test helper) silently disarms every treasury guardrail, and the "predating tests" currently exercise a confirm path that is NOT the production system. **Fix: make Treasury mandatory — remove both nil-checks so a nil Treasury panics-or-errors at first use (fail closed, never skip); update the predating tests to construct the real treasury service (seeded guardrail defaults are generous — they will still pass); the safety rule is arm-or-refuse, never skip-if-absent (ENGINEERING_STANDARD BC-5 / reachability invariant).**
Bonus catches verified: 0014 seed fix (programme CERTIFICATION had ACTIVE-filtered jobs silently skipping the demo programme — the fail-closed gate surfaced it); two M2e error families now mapped at the boundary instead of opaque 500s.
Builder continues M3e (posting-template engine + reproducible settlement); M3D-F1 must close before G3 alongside nothing else — the register is otherwise clean.

### VR-19 — 18 Jul 2026: M3D-F1 verified CLOSED (395f4de) + M3e interim review (70533c5) — CLEAN
Independent 19-package -race suite green (new settlement package).
**M3D-F1 ✓** dedicated fold commit "structurally un-disarmable": zero `Treasury != nil` guards remain in origination (mechanically verified); Treasury mandatory; predating tests construct the real treasury service.
**M3e verified:**
- CFG-012 symbolic balance proof is mathematically sound: symbols as vectors in (DISBURSED, FEE, AMOUNT) basis, OUTSTANDING = (1,1,0) — reviewer independently confirmed the identity outstanding = disbursed + fee holds in BOTH fee models (upfront: repayment=face=disbursed+fee; added: repayment=face+fee=disbursed+fee), so component-wise debit=credit equality covers every binding and every omit_when_zero branch without enumeration. Accounts cross-checked against the ACTIVE chart at approval (closes the template-names-missing-account armed-but-dead).
- All 7 event types render from governed `ledger.templates`; no bypass path; every journal pins its template version. Strongest regression evidence accepted: the entire pre-existing money test net passed UNCHANGED over template-driven postings.
- Settlement ledger-derived (one set-based query, never book tables); exact-partition share arithmetic (telco via PercentBps single rounding site, platform = fee − telco — no vanishing penny, test-asserted incl. half-up tax); finalised statements content-hashed; VerifyReproducible regenerates under PINNED terms version; adversarial tamper test proves an admin-tampered journal makes reproduction fail loudly.
Both M1-era deferred-register items now closed (posting-template engine; DD-19 done earlier).
**DEON status checked (web):** judgment still scheduled 20 Jul 2026 (WASPAN v FCCPC, FHC/L/CS/760/2026); enforcement suspended in the interim — A-5 posture unchanged; re-review fires on the ruling text.
Remaining: M3f (breaks workflow, complaints, dormant bureau, operator surface) → M3 push → **G3 full gate** (M3B/M3D folds re-confirmed at gate, live Render verification, settlement golden tests, DEON re-review if ruling landed).

### VR-20 — 18 Jul 2026: **G3 FULL GATE — PASS** (M3 series → aedeaf6). Record: build/reviews/REVIEW-5-G3.md.
Everything run/read by reviewer directly. 20-pkg -race green (collections/settlement/ops new). **LIVE deployed verification** (head aedeaf6 live): full money story over https://bridgextra-api.onrender.com — offers(governed-priced)→confirm 201 ACTIVE→replay 200→recovery ALLOCATED closed→status CLOSED ₦0. **worker -invariants against PRODUCTION DB → exit 0 "all invariants hold — ledger balances at this instant"** (load-bearing evidence). worker -recon → 1 break (missing_telco) = recon engine WORKING (detected demo-traffic-vs-sim discrepancy, recorded, refused force-match, exit 1); worker -breaks → no aged breaks (unaged, fail-closed threshold). Zero-config-floor sweep verified across all M3 validators (write-off/guardrail/settlement/delinquency/aged-break/CFG-012). M3B-F1+M3D-F1 confirmed closed (VR-17/19). **DEON: system date 18 Jul, ruling still scheduled 20 Jul (not landed) — A-5 re-review fires on ruling text; conservative superset = no code impact any outcome.** Zero open findings. Carried non-blocking: G1-N1/N2, G2-N1/N2, engine-archive(M5), RA-1 branch-protection(pre-go-live). **Builder CLEARED to M4 portals (Next.js admin/risk/finance/ops/support, server-side RBAC+tenant scoping). Next gate G4 = reviewer attempts cross-tenant/cross-role fetches vs BFF.**

### Reviewer-model provenance (owner decision, 18 Jul 2026)
The reviewer seat runs on **Fable 5 or Opus 4.8**, switched by the owner per credit availability — both top-tier, both fully capable of this standard. Continuity is protected by design, not by model memory: every finding is backed by a reproducer test in CI, every gate's evidence + exact commands are recorded here and in build/reviews/, so any capable model (or a human/external auditor) can re-verify from the record. Hygiene on any seat swap: the incoming reviewer re-reads the standing findings + open carried notes + the next gate's criteria before proceeding. Each VR-N entry from here stamps the model that produced it.

### VR-21 — 18 Jul 2026: M4a interim SECURITY review (ba8f66c + gosec fix c519497). 3 MED + 1 LOW. Record: build/reviews/REVIEW-6-M4a-interim.md
Portal auth reviewed at source; portal RBAC/CSRF/logout/maker-checker tests re-run by reviewer — all PASS. Verified correct: 256-bit crypto/rand session tokens stored sha256-hashed (DB leak ≠ session leak); expired/revoked/absent all → identical 401 no-oracle; CSRF per-session constant-time on all mutations; cookie HttpOnly+Secure-UNCONDITIONAL(G124 fix, right call)+SameSite=Strict+Path-scoped; deny-by-default (route absent from routeRoles → 403 all); maker-checker now via real session actor (security UPGRADE over header API); logout revokes server-side. gosec fix c519497 correct (Secure hardcoded, env knob removed — same armed-but-dead pattern killed on treasury; standard self-propagating).
**Findings:** M4A-F1 MED (security, FOLD BEFORE further portal work): Resolve checks only session row, NOT admin_credentials.status — a revoked/offboarded admin keeps a LIVE 8h session with money-config power. Fix = Resolve/VerifyCSRF join admin_credentials status='ACTIVE' (1-line) or revoke-all-for-actor on offboard. M4A-F2 MED (drift): route→roles in 3 unsynced copies (routeRoles map / mux+rbac literal / test slice) — deny-by-default proven for 6 routes but route #7 could mount mismatched/untested; fix = mountRBAC helper single-source + test iterates routeRoles. M4A-F3 MED (forward): RBAC role-only, NO telco/programme scope — fine for M4a global config, but V2-UI-001/TEN-006 need scope+role before M4b tenant screens; design scope into portal_sessions NOW. M4A-F4 LOW: no rate-limit on /v1/portal/login (mitigated by key entropy; M5).
NOT gate-passed (interim). G4 = adversarial cross-role+cross-tenant at M4 completion. **Date is Jul 18 not 20 (builder narration drifted); DEON ruling NOT yet landed, A-5 still pending.** Builder: fold F1 now, F2/F3 in M4b, F4 hardening.

### VR-22 — 18 Jul 2026: M4A-F1/F2/F3 folds VERIFIED CLOSED at source + test (M4b, 230c2f3). No new blocking findings.
Reviewed every fold at source (no cut corners per owner directive) + independently re-ran the full portal suite (6 tests incl. new F1/F2/F3) under -race — all PASS.
- **M4A-F1 CLOSED:** Resolve AND VerifyCSRF now `JOIN admin_credentials a ON a.actor=s.actor ... AND a.status='ACTIVE'` — a REVOKED credential's live session dies immediately, not at 8h TTL. TestM4A_F1_OffboardKillsLiveSession proves offboard→401 at once.
- **M4A-F2 CLOSED:** `mountRBAC` single-sources route→roles and PANICS at boot if a mounted pattern has no routeRoles entry (fail-closed); all 9 routes (incl. 3 new M4b: overview/versions/{id}) present with sane role sets (reads=ADMIN/RISK/FINANCE, writes=ADMIN); RBAC matrix test iterates the production map so it cannot drift.
- **M4A-F3 CLOSED & COMPLETE:** scope designed into admin_credentials + portal_sessions (mig 0018; grammar `*`|global|programme:X|telco:Y; seeded creds default `*` so none silently loses access). PermitsRead (`*` OR record=global OR session==record) / PermitsWrite (`*` OR session==record — writing global needs `*`) — correct asymmetry. **Enforcement verified on EVERY config path:** draft→PermitsWrite(req.scope); submit/approve/activate→loads record, PermitsWrite(cv.scope); active→PermitsRead; get→PermitsRead + no-oracle 404 on cross-scope; versions→handler 403 on foreign scope param + repo hard-bound; overview (AGGREGATE)→SQL `($1='' OR scope=$1 OR scope='global')` — traced the query, NO cross-scope leak. List SQL applies restrict as an independent hard AND (defense-in-depth even if scope param manipulated). TestM4B_F3_ScopeEnforcement proves read-global+own, refuse-others, no-overview-leak.
**Forward G4 notes (not findings — nothing to fix yet):** (a) scope currently gates CONFIG only; when M4d finance/M4e ops surface tenant DATA (settlement/advances), the SAME scope filter must apply to those queries — G4 will attack every tenant-data portal query. (b) sessions bind to `actor` not credential_id — revoking one of an actor's multiple keys won't kill sessions if another key stays ACTIVE; fine for 1-key-per-actor, consider credential_id binding at hardening (LOW).
CI green on 230c2f3. **Builder CLEARED for M4c (risk workspace).** Reminder: date is Jul 18; DEON ruling not yet landed (A-5 pending). Open owner decision: historical-lending-intake sequencing (HISTORICAL_LENDING_INTAKE_SPEC.md).

### VR-23 — 18 Jul 2026: M4c interim review (f0135eb) — risk workspace = portal's FIRST tenant-data surface. Pattern CORRECT; 1 MED structural rec (raise now, before M4d replicates).
Reviewed the scope→filter translation at source (the seam I flagged in VR-22) + ran M4c tests under -race — both PASS (two-person re-arm journey + cross-tenant-hidden).
**Verified correct — traced every scope form:** operator reads run on the BYPASSRLS ReadPool (deliberate — a platform operator legitimately spans telcos; RLS stays on the subscriber app role). `TenantFilter` returns (telco, programme, authority): `*`→("","",true)=see-all; `telco:T`→(T,"",true); `programme:P`→("",P,true); `global`/unknown→("","",false). The list handler checks `if !authority { return empty }` BEFORE querying — so the empty-bounds-means-all SQL can NEVER be reached by a non-admin (the exact see-all-vs-see-none trap, handled correctly). `ListOpenTrips` SQL = hard AND `($1='' OR telco_id=$1) AND ($2='' OR programme_id=$2)`. Action path (loadTripScoped) = `PermitsTenant(telco,programme)` + no-oracle 404. All 3 read paths (list + 2 action-loads) scope-checked. Two-person re-arm flows through the M3d treasury service (self-approve 409, distinct approver, programme→ACTIVE). Good judgment: did NOT hardcode 2-dp major-unit money display — flagged governed currency-decimals as a follow-up (correct no-hardcoding instinct).
**M4C-F1 (MEDIUM, structural — adopt BEFORE M4d):** the scope filter is CONVENTION-enforced, not structurally. Repo read fns take bare `(telco, programme)` strings and the authority short-circuit lives in the HANDLER — so a future M4d/e financial query that forgets the authority check or passes `("","")` leaks cross-tenant **with no RLS backstop** (ReadPool is BYPASSRLS). Today's 3 paths are correct, but the pattern is about to be copied across finance/ops/support. **Recommend: make the scope filter non-bypassable at the repo boundary** — operator read fns take the `PortalSession` (or a `ScopeFilter` type) and derive bounds internally; authority=false returns empty FROM THE REPO, not the handler. One enforced chokepoint (like mountRBAC did for routes) so an unscoped operator read is structurally impossible to write. Cheap now (1 pattern), expensive after M4d/e/f hand-scope a dozen queries. G4 will otherwise require auditing every portal read query individually.
CI green on f0135eb. Builder cleared to continue, but **fold M4C-F1 into M4d's foundation** (M4d is the finance workspace = most tenant-financial-data, the highest-stakes place to inherit a convention-only guard). Full 21-pkg suite green at M4b (VR-22); M4c adds handler tests only.

### VR-25 — 18 Jul 2026: EXTERNAL REVIEW assessed + verified at source. 1 HIGH CONCURRED (reviewer miss, owned).
An external reviewer audited the portal. I verified every load-bearing claim at source before concurring. Disposition:

**EXT-1 (HIGH — CONFIRMED at source; TOP pre-merge blocker; I MISSED it):** the legacy header-auth config API `/v1/admin/config/{drafts,submit,approve,activate}` is still mounted (`cfgAdmin.Mount(mux, adminAuth)` main.go:96) and its `AdminAuth.Wrap` (configadmin.go:33) calls role-UNAWARE `ResolveCredential` with NO role/scope check, routing to the SAME configsvc the portal uses. Since M4a/0017 added roles and M4b provisions RISK/FINANCE/OPS/SUPPORT into the same admin_credentials table, ANY active non-admin credential can mutate+activate money-config via the legacy door — bypassing BOTH the portal ADMIN-only gate AND the M4C-F1 scope model I just verified "structurally non-bypassable" (true for the portal path; there was a SECOND unscoped door I didn't audit). Exploitability gated on a non-admin credential existing (demo seeds only ADMIN today) but structural + live the instant M4b's scoped operators are provisioned. **My miss:** I rigorously reviewed the NEW portal surface but did not re-audit the PRE-EXISTING admin API against the NEW role/scope model. Lesson: when a security MODEL changes, re-audit ALL surfaces touching the resource, not just new code (cf. feedback_fix_regression_pattern "test the invariant on OTHER paths"). **Fix:** remove legacy mutation routes; any future config automation = dedicated service principal through the central RBAC registry; add a structural test asserting NO config-mutation route exists outside mountRBAC (recurrence-proof, ties to EXT-8/route-registry).

**NEW + REAL (adopt):**
- EXT-2 (MED): F1 checks status live but ROLE is a login snapshot → role-downgrade doesn't reduce a live session. Fix: return live role from the F1 join + revoke-sessions-on-role-change.
- EXT-3 (MED, VERIFIED): config_versions broad `INSERT,UPDATE` grant (0001:204, builder comment "revisit at M4 RBAC"), no immutability trigger. Fix: column-scoped lifecycle grant (builder already uses this on outbox/advances) + trigger forbidding content/hash/domain/scope/version_no/created_by mutation post-submit + DB negative test.
- EXT-4 (MED, VERIFIED): 0 validators use DisallowUnknownFields → typo'd field (fee_bp vs fee_bps) silently accepted while prefilled old value persists = armed-but-dead misconfig. Fix: one shared strict decoder (reject unknown/trailing/oversize).
- EXT-5 (MED-LOW): config content_hash = sha256(raw bytes) but stored JSONB normalises → hash not reproducible from stored content. Fix: canonicalise-before-hash + store canonical (builder has this pattern in settlement + ledger lines_hash).
- EXT-6 (MED): telco.adapter fulfilment_url only URL-format validated → SSRF (metadata/internal endpoints), worse under EXT-1. Fix: per-telco allowlist + egress gateway + DNS/IP validation + mTLS pin + no-redirects + maker-checker on endpoint change.
- EXT-7 (MED ops): /healthz does DB ping but is liveness → DB blip causes restart loops. Fix: split /livez (no deps) + /readyz (deps) + /version.
- EXT-8 (MED process): NO frontend CI job (broken portal merges green); tool versions `latest` (caused the G124 surprise). Fix: portal CI job + pin golangci-lint/govulncheck/gosec.
- EXT-9 (MED): durable audit gaps — draft creation, login/logout, session revocation, role/status changes, failed authz, sensitive reads, endpoint changes. Fix: expand durable audit (V2-OBS-005/SEC-009).
- EXT-10 (LOW-MED): M4b completeness — SCHEDULED contract drift (frontend union + overview pending-count omit it); missing content-diff/history; role-aware UI (hide mutation buttons via /me, keep server enforcement); a11y clickable <tr>.

**ALREADY TRACKED:** runtime privilege (owner DSN in API + worker-as-owner) = DEPLOYMENT.md before-production + G1-N3/G2-N2 — absorb the external reviewer's sharper decomposition (split migration job, API gets app-role only, config mutation behind dedicated role) into the go-live gate. Clean commit-bound gate snapshots = already my gate practice (they saw a mid-work snapshot).

**STALE (already fixed in M4b/c — external reviewer had an older snapshot + couldn't run the Go suite):** "revoked sessions retain access" = M4A-F1 FIXED (status join, VR-22); "three sources of truth for routes" = M4A-F2 FIXED (mountRBAC, VR-22); "scopes don't exist in sessions" = M4A-F3 + M4C-F1 FIXED & STRUCTURAL (VR-22/24). Their richer route-registry (add MutationClassification) is worth adopting as the EXT-1 recurrence guard.

**Priority order:** EXT-1 (remove/secure legacy API + recurrence test) FIRST, before ANY M4d work. Then EXT-3/EXT-4 (config integrity), EXT-2, EXT-6, then EXT-7/8/9/10 across M4 hardening, EXT runtime-privilege at go-live. All these fold before G4. Builder was at a clean pause; EXT-1 reopens the register at HIGH.

---

## DEEP ADVERSARIAL PASS (VR-26, 18 Jul 2026) — triggered by EXT-1 escaping ~24 review cycles

**Owner directive:** these should not have escaped our reviews. This pass hunts the EXACT bug class (a sensitive op reachable through >1 door where only one enforces authz) across the WHOLE surface + audits the process that missed it.

### Systematic inventories run (now MANDATORY at every gate — see checklist below)
**A. Full route inventory × auth-per-route.** Result: config mutation has TWO doors (portal ADMIN+scope; legacy `/v1/admin/config/*` role/scope-UNAWARE = EXT-1). The legacy `GET /v1/admin/config/active` is also unscoped → **EXT-1 blast radius EXTENDED: cross-scope config READ leakage, not just mutation.** All other sensitive ops (advance origination, recovery ingestion, guardrail re-arm, write-off) = single-door, correct auth. **EXT-1 is the ONLY parallel-door authz bypass — blast radius bounded to config.**

**B. Full DB grant audit × stated invariants.** Append-only core VERIFIED correct: journals/journal_entries, consents, settlement_lines, recon_break_actions, audit_events = SELECT,INSERT only. **DAP-1 (NEW, MED — generalizes EXT-3):** tcp_worker has column-scoped UPDATE on money tables, but **tcp_app holds BROAD `UPDATE`** on config_versions, advances, funding_pools, programmes — so immutable columns (config content/hash/version, advance face_value/disbursed, pool committed, programme telco_id/code) are NOT structurally protected against the app role. Fix: column-scoped UPDATE grants on tcp_app matching the worker discipline + immutability triggers on truly-immutable columns. (config_versions = sharpest instance = EXT-3.)

### RETROSPECTIVE — why EXT-1 escaped 24 cycles (the real deliverable)
1. **Reviews were slice-oriented, not surface-oriented.** Each gate verified the NEW code in that milestone. The legacy config API was CORRECT when built (M1a, pre-roles) and became a bypass when the MODEL changed (M4a added roles/scopes). No gate re-audited the FULL route surface against the CURRENT model — only the diff.
2. **No structural guard against parallel doors.** mountRBAC guards portal routes; the legacy API mounts OUTSIDE it. Nothing asserted "every config-mutation path goes through the central authz registry."
3. **"Verify the control works" ≠ "verify it's the ONLY path."** I traced each new control's SQL and ran its tests — deep but narrow. The adversarial question "what ELSE reaches this resource?" was not on the checklist.

### PROCESS FIX — new MANDATORY gate checklist (added to every G-review from here)
- **GATE-CHK-1 Surface re-audit:** enumerate ALL routes + ALL grants and check each against the CURRENT security model — every gate, not just the diff. (Inventories A+B above are the template; re-run each gate.)
- **GATE-CHK-2 Parallel-door check:** for every sensitive resource (config, advance, recovery, ledger, funding, sessions), list EVERY path that reaches it and confirm all enforce the same authz.
- **GATE-CHK-3 Structural single-door test:** a test asserting no sensitive mutation reaches its usecase (configsvc.Approve/Activate, treasury re-arm, ledger.Post, etc.) except through the central RBAC/authz chokepoint — recurrence-proof, the EXT-1 permanent fix.
- **GATE-CHK-4 Immutability-by-grant:** every table with a stated immutability/append-only invariant must be SELECT+INSERT or column-scoped-UPDATE for BOTH roles — audited at each gate (DAP-1 is the first sweep).

**Net:** EXT-1 confirmed as the sole parallel-door bypass (bounded to config, incl. reads); DAP-1 new (grant immutability class); append-only money core verified sound. The four GATE-CHK items now run every gate — G4 included, where I re-attack with "is this the only door?" as the lead question.

### VR-27 — 18 Jul: EXT-1 task brief issued + honest coverage map + remaining sweeps planned
- **EXT-1 brief** = build/TASK_EXT1_legacy_config_api.md (remove legacy config routes + GATE-CHK-3 recurrence test + bundle EXT-3/DAP-1 grant lockdown + EXT-4 strict decode). TOP blocker before M4d.
- **Honest assurance calibration (told owner):** this deep pass FULLY closes the parallel-door + grant-immutability classes; prior gates verified money/RLS/saga/portal-authz. NOT yet independently swept to equal depth: (1) input-validation completeness, (2) all outbound/SSRF surfaces (adapter + notifications + bureau), (3) PII-in-logs (V2-SEC-008), (4) authn rate-limit/lockout, (5) frontend client (XSS/portal). External review poked several (EXT-4/6/8/9) = findings adopted, but adopting ≠ systematic sweep.
- **Plan:** before G4, run one focused systematic sweep per ⚠️ dimension (same treatment as the parallel-door pass). PLUS recommended to owner: independent professional pen-test before pilot — no single reviewer is sole assurance for a lending/NDPA platform (two passes finding real HIGHs between them = argument FOR more eyes).

### VR-28 — 18 Jul: EXT-1/3/4 cluster VERIFIED CLOSED at source + test (012c352, CI green). No new findings.
Verified: EXT-1 door GONE (no legacy /v1/admin/config routes, AdminAuth/ConfigAdmin removed, role-unaware Admins.ResolveCredential deleted; channel Telcos.ResolveCredential correctly preserved — portal is now sole config surface). GATE-CHK-3 = 3 tests incl. a POSITIVE CONTROL (ScannerCatchesADoor builds the synthetic EXT-1 defect + asserts the AST scanner catches exactly 1 — proves the guard fires, not vacuously green); scanner correctly sanctions mountRBAC's variable-pattern mount, flags literal config-route mounts. EXT-4 strict decoder on all 19 validators, typo-rejection test + it cleanly surfaced the validate-new-draft vs project-trusted-config distinction (activeChartAccounts stays lenient — right call). EXT-3/DAP-1 mig 0019 = column-scoped grant + owner-proof immutability trigger, BOTH layers tested + positive control. All PASS in my run.
Minor hardening notes (not findings): GATE-CHK-3 scanner catches literal-pattern config doors + scans handler pkg + api/main.go — a variable-pattern direct mount or a route mounted from a new wiring file would need the scan-surface extended; acceptable (realistic copy-paste reintroduction is caught). 
**NEXT BUILD STEP = M4d (finance workspace) on the hardened OperatorScope+config foundation.** In parallel I run the 5 dimension-sweeps, FRONT-LOADING PII-in-logs + input-validation (M4d exposes ledger/settlement = most sensitive data, so findings fold into M4d not retrofit); egress/login-hardening/frontend sweeps complete before G4. Owner not pausing.

### VR-29 — 18 Jul: M4d part-1 ledger browser verified + PII-IN-LOGS SWEEP (dimension 1 of 5) — PASS
**M4d part-1 (ledger browser, 03ebc9d, CI green):** verified at source + test (TestM4D_LedgerBrowser_ScopeAndTapThrough PASS in my run). Finance routes go through mountRBAC (deny-by-default, {ADMIN,FINANCE}); reads take sess.OperatorScope() with the `!scope.authority` short-circuit enforced IN THE REPO (portalfinance.go) — the M4C-F1 structural pattern inherited cleanly, NO regression to convention on the money-read path. No client-side money arithmetic. GATE-CHK-2 (parallel-door): journal reads reachable only via portal finance; ledger.Post is worker-only (append-only grant) — single door. Good.
**PII-IN-LOGS SWEEP (V2-SEC-008) = PASS.** Enumerated every backend log field. NOT logged anywhere: full MSISDN, msisdn_token (even tokenized), NIN, api_key, session/CSRF token, password, key_hash, financial amounts. Logged = operator identity (actor/role — staff), internal IDs (advance/telco/programme/event/run/statement), counts, source IP, err strings, path. Tokenize-everything discipline extends to logs — better than V2-SEC-008 requires. **LOW residual (PII-LOG-1):** `"err", err` logged broadly — an error string COULD carry PII if a downstream error embeds a token/amount; saw no such pattern (errors wrap IDs); recommend a targeted spot-check of error construction on subscriber/recovery/fulfilment paths at hardening. No leak evidence.
**Dimension-sweep progress:** 1/5 done (PII-in-logs PASS). Remaining before G4: input-validation completeness (NEXT), egress/SSRF surfaces, login rate-limit hardening, frontend client. M4d continues: part-2 breaks queue, part-3 settlement verify.

### VR-30 — 18 Jul: M4d COMPLETE — parts 2 (breaks 446c67d) + 3 (settlement b2ec339) verified. No new findings.
All 3 M4d tests PASS in my run (ledger + breaks + settlement). Routes via mountRBAC {ADMIN,FINANCE}. **TelcoLevelBound (new primitive) verified fail-safe at source:** breaks are telco-ONLY; the primitive returns ("", false) for a programme-scoped operator (explicit `if s.programme!="" return "",false`) — a programme operator can NEVER get the ("",true) see-all bound (the leak case); admin→("",true)=all, telco:T→(T,true). Correct conservative design — builder recognized breaks are telco-only (unlike journals telco+programme) and built a distinct primitive vs forcing a wrong mapping. Settlement verify = scope-bounded + genuine verified:true (real FINAL statement recomputed vs pinned hash) + DRAFT→409 + cross-scope 404, all tested. GATE-CHK applied: single-door reads, no client money math.
**Design note (not a finding):** programme-scoped operator sees NO recon breaks (they're telco-level financial recon spanning the telco relationship, not a programme) — semantically correct for now; if product later wants programme-filtered breaks it needs recon_items to carry programme_id. Flag for G4/product.
**M4 status: M4a auth ✓ M4b config ✓ M4c risk ✓ M4d finance ✓ (3 surfaces). Remaining: M4e ops, M4f support → G4.** Dimension-sweeps: 1/5 (PII-logs PASS); input-validation IN PROGRESS (M4d write endpoints breaks-action/settlement-verify use typed decode — spot-verified, full sweep continues); egress/SSRF + login-hardening + frontend before G4. Usage reset ~9am tomorrow.

### VR-31 — 18 Jul: HONEST CORRECTION — reviewer thoroughness gap; builder self-audit found real issues I missed. GATE-CHK-4 completed properly.
**Owner challenged review thoroughness. Correct to. The builder's self-audit found 3 real issues; 2 are EXTENSIONS of findings I raised but left INCOMPLETE:**
1. **DAP-1 was a SAMPLE, not a sweep.** VR-26 ran the full grant audit — data was present — but I named only 4 immutable-record tables (config_versions/advances/funding_pools/programmes) and MISSED settlement_statements + guardrail_trips (both in my own VR-26 dump). Builder caught them (mig 0020, verified GOOD: FINAL-frozen trigger + column-scoped grants).
2. **GATE-CHK-3 only guards CONFIG routes** — I verified+praised it as "the permanent fix" and downgraded its config-only scope to a "minor" note. Not minor: treasury re-arm / break-action / settlement-verify are equally sensitive portal mutations a parallel door could bypass. Builder generalizing the guard to every /v1/portal/* route (except login/logout) — correct, adopt as standing.
3. **Settlement verify conflated mismatch (verified:false) with operational error** (DB blip → false "tampering" alarm, or trains operators to dismiss real mismatch). I checked scope+genuine-verify at VR-30 but NOT error semantics. Builder fixing with typed error (mismatch→false, opError→500) — correct.

**GATE-CHK-4 COMPLETE immutable-record class sweep (done now, properly):** frozen/contractual/loss/evidence records with broad tcp_app UPDATE = config_versions ✓(0019), settlement_statements ✓(0020), guardrail_trips ✓(0020), **write_offs ✗ GAP** (POSTED=crystallised loss + approver, broad grant, NOT in 0020 — verified: same class, hand to builder for 0020 batch), advances + funding_pools (DEFERRED by builder as FSM-table task — legit but MUST track), programmes (identity cols telco_id/code immutable — DAP-1, still open). Append-only core (journals/entries/consents/settlement_lines/recon_break_actions/audit + decision_snapshots col-scoped) VERIFIED sound.

**REVIEWER PATTERN FAILURE (own it):** reviews were deep-but-narrow — verified each instance thoroughly but repeatedly stopped at a SAMPLE instead of completing the class-wide sweep (GATE-CHK-4 was DEFINED at VR-26 but not EXECUTED to completion). Two independent audits (external + builder self-audit) each found real things = strong signal the single continuous reviewer is NECESSARY-BUT-NOT-SUFFICIENT. **Independent professional pen-test before pilot is now NOT optional — it is required assurance.** Corrective: full surface+grant sweep is a HARD gate item (not sample); generalized GATE-CHK-3 standing; complete every CHK-class sweep to exhaustion, list ALL instances, never a representative few.

### VR-32 — 18 Jul: INDEPENDENT FULL AUDIT (owner-requested) — found issues the external review AND builder self-audit missed.
Ran my own class-complete sweeps (not samples). Compared vs builder findings.

**Dimensions SWEPT TO COMPLETION:**
1. **SQL injection = CLEAN.** All production Query/QueryRow/Exec parametrized ($N). Only fmt.Sprintf-into-SQL is testutil DROP/CREATE DB with regex-validated name (^[a-z0-9_]+$). No dynamic SQL anywhere in prod.
2. **Grant-immutability (GATE-CHK-4) = COMPLETE.** config_versions✓(0019) settlement_statements✓(0020) guardrail_trips✓(0020); **write_offs ✗ GAP (I found; builder self-audit MISSED — POSTED loss+approver, broad grant, not in 0020)**; advances/funding_pools deferred-FSM (track); programmes identity-cols open. Append-only core sound.
3. **SSRF/egress = BROADER THAN EXT-6.** THREE outbound surfaces, EXT-6 named only #1: (1) mno/adapter fulfilment+enquiry, (2) featureingest feature-file fetch — BOTH from telco.adapter.fulfilment_url, (3) notify provider POST (separate config). URL validated approval-time + format-only (absolute http/s), NO allowlist, NO use-time check. Egress control must cover ALL 3. (Severity bounded: config now ADMIN-only post-EXT-1.)
4. **PII-in-logs = CLEAN** (VR-29).
5. **Parallel-doors = config only** (VR-26); GATE-CHK-3 generalization in progress (builder).

**vs BUILDER SELF-AUDIT:** builder found settlement error-semantics (fixed), GATE-CHK-3 generalization (fixing), settlement_statements+guardrail_trips grants (0020 ✓). I independently ADDED: write_offs grant gap (builder missed) + SSRF class broader than EXT-6 (featureingest + notify). Overlap = grant-immutability, I completed it further.

**DIMENSIONS NOT YET SWEPT (honest — need continuation pass, budget-limited this session):**
6. Input-validation completeness (per-handler decode bounds / MaxBytesReader coverage) — PARTIAL.
7. Login rate-limit / authn hardening (EXT-8) — not swept.
8. Error-semantics RECURRENCE (settlement fixed by builder; other verify/check/resolve paths not swept for op-error-vs-business-outcome conflation).
9. Frontend client (XSS / Next.js) — not reviewed.

**GAPS TO CLOSE BEFORE MOVING AHEAD:** (a) write_offs → builder 0020 batch; (b) SSRF egress control across all 3 surfaces (not just adapter); (c) continuation sweep of dims 6-9; (d) independent pen-test = REQUIRED. High-severity classes (injection/grants/SSRF/PII/parallel-doors) now swept complete; money core + ledger + scope model remain sound.

### VR-33 — 18 Jul: builder self-audit fixes VERIFIED (3dd4e99). VR-31 critique CLOSED. But VR-32 findings still OPEN.
Generalized GATE-CHK-3 verified at source: fails build if ANY /v1/portal/ route (login/logout excepted by design) mounts outside mountRBAC; POSITIVE CONTROLS prove it catches treasury re-arm + settlement-verify bypasses (not just config) — my VR-31 "config-only guard" critique FULLY CLOSED. Settlement error-semantics (typed ErrNotReproducible: tamper→verified:false, op-error→500) + 0020 record immutability = verified good (VR-31).
**STILL OPEN from my VR-32 independent audit (builder self-audit did NOT cover):** (1) **write_offs immutability** — builder noted as task #42 but NOT fixed; POSTED loss still mutable via broad grant; (2) **SSRF class = 3 surfaces** (adapter + featureingest [both telco.adapter.fulfilment_url] + notify) — builder unaware; egress control must cover all 3, not just adapter (EXT-6). Plus un-swept dims 6-9 (input-validation, login-hardening, error-semantics recurrence, frontend).
**REVIEWER RECOMMENDATION on "where next": NOT M4e yet.** Close the KNOWN-open foundation findings before more surface sits on them (the exact lesson of this session): (a) write_offs freeze post-POSTED [small, clear — the write_offs slice of #42]; (b) SSRF egress control across all 3 outbound surfaces; THEN (c) broader FSM lockdown advances/funding_pools [larger #42, careful per-usecase column enum]; THEN M4e ops. Platform is NOT "fully clean" until (a)+(b) land. Money/ledger/scope core sound; these are integrity-in-depth. Pen-test pre-pilot REQUIRED.

### VR-36 — 19 Jul (Fable 5): M4e design gate (b8c613d) — APPROVED WITH CONDITIONS. M4e-1 cleared to start.
Design read at source. Architecture correct: enquire-now-only ambiguity queue (no manual confirm/fail — right conservatism, money moves only on telco evidence); parked-reversal retry REUSES applyReversalGuarded (no new money semantics); status action = write-off/re-arm maker-checker pattern w/ 0026 scoped GRANT UPDATE(status) + probe flip (first designed use of the 0025 discipline); demo drives the REAL origination path, allowlist-guarded, no stubs.

**Conditions (fold into build; verify at slice reviews):**
- **C1 REQUIRED (conduct floor in CODE):** ops.status_actions validator must STRUCTURALLY refuse SELF_EXCLUDED as from-status OR to-status — not merely omit it from the seed. Config-governed transition sets mean a future governed change could otherwise add SELF_EXCLUDED→ACTIVE; an ops override of self-exclusion is a conduct violation (EDG-030, V1-CFG-009 "configuration cannot disable statutory controls"). Non-configurable boundary, validator-enforced.
- **C2 REQUIRED (CAS + state guards):** apply step = compare-and-set `WHERE status = from_status` (refuse loudly if drifted since request — concurrent actions/stale approvals); enquire-now guarded to state ∈ {UNKNOWN, SENT} in the UPDATE predicate; parked retry race-safe vs concurrent ingest auto-apply (reuse should give this via PARKED from-state guard — prove with a test).
- **C3 REQUIRED (zero-config floor):** ops.status_actions + ops.queues absent/invalid config = REFUSE the action, never default-allow (fault_demo already states its floor; state it for all three domains).
- **C4 REQUIRED (money-safety statement + test):** BARRED/CLOSED must NOT cascade to open advances — advance FSM and recovery continue (recovery targets advance state, not account status; test proves recovery against a BARRED subscriber's ACTIVE advance still applies).
- **C5 suggest:** partial-unique one-open-status-action per subscriber (guardrail_trips convergence pattern) — else two REQUESTED actions race to approval and the loser only dies at CAS.
- **C6 suggest:** per-run demo token suffixes so one-active-advance doesn't make timeout_unknown a one-shot demo; optional min-interval floor on enquire-now.
- **C7 nit:** pin queue-route roles explicitly in routeRoles (deny-by-default forces it; decide OPS/ADMIN + FINANCE-read-on-reversals deliberately); §5 says "three tabs", lists four.

Slicing approved (M4e-1 queues → M4e-2 status action w/ FULL-suite gate → M4e-3 demo). I review each slice at commit; C1-C4 are acceptance criteria for their slices.

### VR-37 — 19 Jul (Fable 5): M4e-1 ambiguity queues (48761f2) — VERIFIED CLEAN at source + my own runs. 1 LOW carried to M4e-2.
Verified at source: (1) repo reads = non-bypassable OperatorScope (fulfilments via advances join, hard `($n='' OR col=$n)` bounds, authority short-circuit) + TelcoLevelBound on reversals (programme/global-scoped operator reads NOTHING); state-scoped no-oracle loads. (2) C2 real at both actions: EnquireNow CAS `WHERE state IN ('UNKNOWN','SENT')` RowsAffected-guarded; RetryParked claims `WHERE state='PARKED' FOR UPDATE` — row lock serialises operator retry vs ingest auto-apply, loser sees ErrNotFound, NO double-apply path; retry REUSES the exact ingest decision tree (ORIGINAL_UNSEEN → ORIGINAL_NOT_ALLOCATED → applyReversalGuarded → collision refreshes park_reason / MarkApplied drains). Both audited. (3) Load-scoped-then-act: actions derive telcoID from the scope-loaded row, never caller input. (4) C3 floor in code at validator (positive-bounds, strict decode) AND consumer (absent config → OPS_QUEUES_UNCONFIGURED refuse — "an unbounded queue is not an empty one"). (5) C7 pinned: reads ADMIN/OPS/FINANCE, actions ADMIN/OPS, all 4 routes via mountRBAC (GATE-CHK-3 + matrix auto-cover). (6) OpenAPI same-commit; mig 0026 seeds ops.queues.
**My own runs (Docker, real tcp_app):** both TestM4E packs PASS -race; recovery + configsvc + repo suites PASS. CI on 48761f2 re-verified via GitHub API (backend+quality success).
**VR-37-F1 LOW (fold into M4e-2):** the C3 consumer floor has no test proving the refusal fires (packs cover staleness/scope/C2/audit; not config-absent → OPS_QUEUES_UNCONFIGURED). Zero-config-floor standing rule = every floor proven to fire. One test: deactivate/absent ops.queues → GET refuses with the typed error.
**M4e-2 cleared** (mig 0027 status-action maker-checker; acceptance = C1 validator-refuses-SELF_EXCLUDED both positions, C2 CAS apply, C3 floor + floor TESTS incl. VR-37-F1, C4 recovery-vs-BARRED test, C5 partial-unique adopted; FULL-suite gate for the grant change).

### VR-38 — 19 Jul (Fable 5): M4e-2 status action (2b28597) — VERIFIED CLEAN, all conditions C1-C5 + VR-37-F1 delivered with proof. VR-35-F1 CLOSED.
Source-verified, tests run BY ME (Docker, real tcp_app), CI re-verified via GitHub API (backend+quality success):
- **C1 exceeded:** conduct floor structural at TWO layers — table CHECKs admit only ACTIVE/BARRED/CLOSED (SELF_EXCLUDED cannot even be RECORDED as from/to, holds against bad config) + validator refuses SELF_EXCLUDED both positions AND from-CLOSED (terminal) AND distinguishes empty-list (valid refuse-all) from absent-key (refused).
- **C2:** apply = CAS `WHERE status = $2` → typed ErrStatusDrift on RowsAffected 0 (journey test drifts status between request and approval, proves refusal); decision claim `FOR UPDATE state='REQUESTED'` serialises concurrent deciders; transition set re-checked at decision time.
- **C3 + VR-37-F1:** TestM4E_ZeroConfigFloors deletes governed config, proves BOTH floors fire (OPS_QUEUES_UNCONFIGURED + status-action refuse-all). Floor-proven-to-fire rule satisfied.
- **C4:** TestC4_RecoveryAgainstBarredSubscriber_StillApplies — recovery against BARRED subscriber's ACTIVE advance allocates + closes; money path never reads account status.
- **C5:** partial-unique one-open-action index (status_action_one_open_uq); second open request → 409.
- **Mig 0027 born column-scoped** (decision cols writable; identity/transition/requester write-once via trigger; terminal freeze even vs owner — write_offs pattern) + the designed re-grant `GRANT UPDATE (status)` landing WITH its writer+tests = 0025 discipline first designed use, exactly as VR-36 gated. Probe flipped {status:true}, msisdn_token/effective_from stay denied — verified in my probe run.
- **Tenant sourcing structural:** telco-bounded operator acts only in own telco (body ignored); programme/global scope → 403; '*' admin must name telco. Distinct-approver at schema regardless of role.
- **The point, proven:** journey test shows the subscriber actually BARRED and the blocked_statuses gates finally seeing it — the armed-but-dead chain from VR-35-F1 now has a live producer. VR-35-F1 CLOSED.
Note accepted: browser verification skipped (dev-server port conflict) — surface exercised through real-HTTP journey tests; acceptable for this slice, UI look verified at M4e-3/G4.
**M4e-3 cleared** (fault demo; C6 per-run token suffixes adopted). Then M4f support → G4.

### VR-39 — 19 Jul (Fable 5): M4e-3 fault demo (dd08373 + gofmt 4655175) — VERIFIED CLEAN. M4e COMPLETE (#41 closed). M4f cleared.
Source-verified + my own runs (TestM4E3_FaultDemo_EndToEnd, TestM4E3_DemoGuards, probe, featureingest — all PASS -race as tcp_app; CI success re-verified via API):
- **demo_runs immutable-by-grant:** SELECT+INSERT only for tcp_app, SELECT for worker, RLS both directions — no UPDATE grant EXISTS. Run id = idempotency key + correlation id → retry replays, never double-lends; chain queryable by construction.
- **Double structural gate on the run:** demoTelco = same TelcoLevelBound pattern (own-telco operators only; '*' names one; programme-scoped 403) ∧ governed allowlist (seeded SIM_NG only) — nobody reaches a real telco; real-telco operators can't reach the sim without '*'.
- **Validator = both-directions armed-but-dead prevention:** `enabled` required (absent ≠ off); enabled must name ≥1 telco (each w/ programme) + ≥1 scenario w/ non-empty token pool — enabled-but-unrunnable cannot activate; OFF always valid. C3 floor firing proven in TestM4E3_DemoGuards.
- **Real pipeline end-to-end:** sim feature file plants 9 demo-pool rows (outside SPIKY/THIN classes) → real ingest+scoring make them eligible → run drives ordinary origination (genuine offer/advance/ledger). E2E proves the design-gate promise: timeout_unknown → FULFILMENT_UNKNOWN (EDG-005, never guessed) → M4e-1 enquire-now + resolver → CONFIRMED/ACTIVE, all through portal surfaces. C6 pool rotation w/ honest 409 on exhaustion; hard_fail self-frees.
- **Test-weakening check PASSED (verified, not accepted):** featureingest assertion diff read at source — old = token-shape regex count ≥100 (shape-coupled, weak); new = feature_snapshots⋈subscriber_accounts exactly 100/100 (semantic invariant, exact). STRENGTHENED.
- **Process note accepted:** first CI red = gofmt (gate script echoed instead of hard-failing format) — fixed 4655175 + gate scripts hard-fail formats going forward. Honest disclosure, right fix.
**M4e = COMPLETE** (queues VR-37, status action VR-38/VR-35-F1-closed, demo VR-39). **M4f support workspace cleared** — masked subscriber timeline (V2-SUB-008/UI-004: MSISDN masked by default, audited step-up reveal if built; SUPPORT role read-only; support must NOT alter financial truth per V3-ORG-005). Then G4 full adversarial gate (cross-tenant/cross-role attacks vs BFF, GATE-CHK-1..4 full re-audit, UI eyes-on). Standing: pen-test pre-pilot; dims 6-9 sweeps front-loaded at G4.

### VR-41 — 19 Jul (Fable 5): THIRD external audit (Deep Engineering Audit, commit 14e0802) — REVIEWER CONCURS. G4 downgraded to CONDITIONAL-pending-Gate-A. Consolidated package: build/CONSOLIDATED_REMEDIATION_2026-07-19.md.
Owner relayed an independent deep engineering audit. I verified its money-path findings AT SOURCE and CONCUR — several are defects I MISSED across ~40 cycles. Honest root cause: M4 reviews portal-focused; accepted M1b-5 recon as walking-skeleton without adversarial re-audit; and my OWN VR-32 SSRF remediation was under-counted (3 doors, missed recon as the 4th — my egress header literally says "three doors").

**P0 money/command-integrity — ALL CONFIRMED at source (I did NOT find these):**
- AUD-P0-001 confirm idempotency: replayByIdemKey blind-loads by (telco,idem_key), NO request-equivalence (offer/programme/token) check; idempotency_records abstraction exists but unwired. Violates API-002/003 ("original result for VALID retries"), ADV-002/006. → R-P0-1.
- AUD-P0-002 recovery dedup: (telco,source_event_id) only, on-conflict "touch nothing" no amount/currency/hash compare; thin replay response (no Applied/Excess/Closed). → R-P0-2.
- AUD-P0-003 recon currency-blind: platformRecord has NO currency; match = abs64(FaceValueMinor delta) only; NGN1000==GHS1000. → R-P0-3.
- AUD-P0-004 recon overflow: abs64(p-t) on external telco JSON; subtraction + abs64(MinInt64) overflow; M2 ceiling doesn't cover this path. → R-P0-4.
- AUD-P0-005 recon SSRF 4th door: recon.New plain http.Client not egress.SafeClient — EXTENDS my VR-32 (I closed 3, missed this). → R-P0-5.
- AUD-P0-006 recon not production/run-idempotent (concur, architectural — M1b-5 status). → R-P0-6 (own workstream).
- AUD-P0-007 consent inferred: Channel:"USSD" hardcoded (origination.go:460), no disclosure-hash/session evidence; proves offer existed not that terms were rendered+accepted. REG-002/PRD-002-003/EDG-028. → R-P0-7.
**Severity reconciliation:** AUD-P0-008 (portal SSO/MFA) — I split: rate-limiting slice = P0 (=my G4-F1, SEC-011/TEN-004 R1-MUST), SSO/MFA slice = P1 (controlled pilot can run seeded-creds+rate-limit). → R-P0-8 + R-P1-8.
**Reviewer G4 findings merged:** G4-F1 rate-limit = R-P0-8; G4-F2 health-split = R-P1-16 = AUD-P1-018 = EXT-7 (3-way concur).
**P1/P2:** ~24 P1 + ~23 P2 triaged into R-P1-1..23 / R-P2-1..11; many already tracked (P1-001=my VR-22 note-b; P1-002=my M4c BYPASSRLS pattern, upgraded accepted-risk→P1; P1-012=EXT-5 open; P2-001/007=#45+M4-depth). Spot-verified P1-016 (amount_minor:number), P2-002 (token in URL query), P2-004 (complaint actor=channel:*).
**PATTERN CONCLUSION:** 3rd independent audit (VR-25 external, builder self-audit, this) each finding distinct REAL money-path/security issues = single continuous reviewer NECESSARY-NOT-SUFFICIENT is now proven, not asserted. Independent pen-test = hard prerequisite. NONE touches append-only ledger / balance invariant / tenant-authz / money representation — those sound (auditor §4 agrees). No rewrite.
**GATE STATUS:** G4 CONDITIONAL PASS → held as CONDITIONAL-pending-GATE-A. I do NOT sign G4 fully passed until the money-command P0s (R-P0-1..5,7,8) close with adversarial tests, re-verified at source + my own runs. Merged backlog in CONSOLIDATED_REMEDIATION_2026-07-19.md; builder Sprint 1 = the P0 money integrity set.

### VR-42 — 19 Jul (Fable 5): Gate A slices 1-3 (R-P0-1/2/3/4/5) RE-VERIFIED at source w/ FALSIFICATION lens + my own adversarial runs. CLEAN. These were MY misses — deepest scrutiny applied.
Re-verified the exact findings I missed, testing the adversarial COMPLEMENT of each invariant (not the happy property):
- **R-P0-1 (daaf2c6):** PutIfAbsent is the SINGLE chokepoint, FIRST in tx1, DB-PK-arbitrated (INSERT ON CONFLICT DO NOTHING). Falsification q: do the TWO residual replayByIdemKey branches (errReplayRace, ErrConcurrentAdvanceBlocked) bypass the hash? NO — reaching them requires stored=true (this call claimed its OWN hash), so they cannot serve a divergent request; the divergent concurrent case resolves THROUGH PutIfAbsent's hash compare. Confirmed by TestRP01_ConcurrentSameKeyDifferentBody_DivergentRefused passing. Divergent → rollback + out-of-band audit + ErrDivergentDuplicate(409).
- **R-P0-2 (a4c44c1):** hash covers amount/currency/token/occurred_at (the auditor's fields). Falsification q: is "exact original outcome" real or thin? REAL — first path classifyAndApply→encodeIngestResult→SetResponse(200,body) in-tx; replay decodeIngestResult(rec.ResponseBody). Test-helper change (Now()→fixedOccurredAt) is NOT a weakening: models a real telco replay (carries own timestamp); divergence still driven by amount/token, proven by TestRP02_DivergentAmount/Token_Refused. 0029 write-once trigger on response cols.
- **R-P0-3/4/5 (3e05174):** ordering correct — malformed/out-of-range telco input (isISOCurrency + credibleAmount[0,ceiling]) → BREAK_MALFORMED_TELCO_RECORD BEFORE any subtraction (overflow impossible); then currency BEFORE amount → BREAK_CURRENCY_MISMATCH. Ceiling = governed recon.tolerance.max_amount_minor (0030 supersede). Recon HTTPClient now egress.SafeClient — the 4th door I missed; only .Do uses it, no plain client left.
**My own runs (Docker -race fresh-DB 30 migs):** origination pack incl TestRP01_* (SameKeyDifferentOffer/Token→divergent, ConcurrentSameBody→one-advance-replay, ConcurrentDifferentBody→refused, CrashAfterCommit→replay-not-double-lend) ALL PASS; recovery pack incl TestRP02_* + EDG-018-still-green ALL PASS; recon pack TestRP03/04/05_* (currency/overflow/malformed/SSRF-metadata-refused) ALL PASS. CI green all 3 commits.
**Verdict: 5 of 8 Gate A P0s CLOSED, reviewer-re-verified.** Remaining: R-P0-8 (rate-limit login+channel+adapter breaker), R-P0-7 (consent disclosure-evidence token, coord DD-06), R-P0-6 (recon framework, own workstream). Builder cleared to proceed R-P0-8 next; I verify each as it lands. G4 stays CONDITIONAL-pending-full-Gate-A until R-P0-6/7/8 close.

### VR-43 — 19 Jul (Fable 5): R-P0-8a rate limiting (7c0dbf1) — VERIFIED, 1 MEDIUM finding by falsification. Login clean; channel key bypassable.
Verified GOOD at source + my runs: Limiter.Allow fail-closed (unknown scope / non-positive limit → DENY — reachability invariant, correct); config-governed (platform.ratelimit, 0031) with cmd/api boot-exit if absent + Mount panics on nil (mountRBAC fail-closed idiom); rate-limit wraps BEFORE auth on both surfaces (login before credential check, channel before TenantAuth/DB lookup); login keyed by clientIP=RemoteAddr (NOT X-Forwarded-For → no header-spoof bypass; on Render behind proxy this collapses to a global bucket = crude but real global brute-force cap, honestly tracked as R-P2-7). ratelimit unit pack + TestRP08_Login_RateLimited PASS in my -race run; CI green.

**R-P0-8a-F1 — MEDIUM (bypass in a P0 fix; must close before Gate A signs): channel rate limiter is bypassable by credential rotation.** channelRateKey returns `"cred:"+apiKey` whenever ANY api-key header is present (only falls back to IP when NO key). An attacker rotating a DIFFERENT invalid key each request lands in a fresh per-key bucket every time → never throttled → each request proceeds to TenantAuth's DB lookup (sha256 + indexed SELECT). This DEFEATS the slice's own stated intent ("hammering with a bad credential is throttled before any DB lookup") — true only for a REUSED key. The IP fallback is unreachable for any attacker (they always present a key). Code + tests share the blind spot: TestPerKeyIsolation proves distinct keys get distinct buckets (the intended per-telco fairness) but never tests the adversarial complement (rotating attacker-controlled keys = the bypass) — same verify-not-falsify pattern I owned at VR-41.
**Fix:** always-on IP-keyed PRE-auth throttle on the channel surface (a rotating attacker shares one IP bucket), independent of the per-VALIDATED-telco fairness bucket which should apply AFTER credential resolution. Hard dependency on **R-P2-7** (trusted X-Forwarded-For) — without it IP is proxy-collapsed on Render and neither login nor channel IP-keying is per-client-meaningful. Add a channel-credential-rotation test (distinct bad keys → still throttled by IP). **Elevate R-P2-7 from P2 to a Gate-A dependency of R-P0-8.**

R-P0-8b (adapter circuit breaker) split is legit — circuit_error_threshold_pct/circuit_min_requests are validator-required but unread by adapter.go (armed-but-dead, my [[reachability-invariant]] class); wiring needs a cooldown field + seed supersession. Own slice, fine.
**Gate A: 5.5 of 8 P0s. R-P0-8a lands WITH F1 open (fold into 8b or a follow-up before Gate A close). Remaining: R-P0-8a-F1, R-P0-8b, R-P0-7, R-P0-6.**

### VR-44 — 19 Jul (Fable 5): R-P0-8a-F1 + R-P2-7 (f0e6205) — VERIFIED CLEAN by falsification. Bypass structurally closed.
The fix for a bypass can re-introduce it via a spoofable IP derivation — so I attacked the derivation first. Verified at source:
- **Trusted-proxy client-IP = rightmost-N-trusted (spoof-safe):** `idx = len(parts) - trustedHops` → with 1 hop reads the entry the Render LB APPENDED (real client); attacker-injected XFF entries sit LEFT and are ignored. Fail-safe fallback to RemoteAddr on idx<0/empty. Correct under a correctly-set trusted_proxy_count (comment: direct deploy must set 0). Config validator requires trusted_proxy_count>=0 + all 3 surfaces; 0032 supersede.
- **Two-tier ordering correct:** `channel_ip` (IP-keyed) wraps `auth.Wrap(...)` → rotating-key flood refused PRE-auth by IP bucket, before any DB lookup. `perTelcoRateLimit` runs INSIDE auth.Wrap, keyed on `platform.TenantFrom(ctx)` = the VALIDATED telco → a forged key can NEVER mint a bucket (TenantAuth 401s it first). Fail-safe: no tenant post-auth → 500 refuse, never bypass.
- **The complement tests now EXIST (the ones the original slice lacked):** TestClientIP_TrustedProxy/{direct_spoof_ignored, render_one_hop, spoofed_prefix, two_hops, no_xff} + TestRP08F1_RotatingInvalidKeys_StillThrottledByIP (30 distinct forged keys, one IP → 429). ALL PASS in my -race run; login pack still green; CI green.
Falsification questions all resolve safe: rotating-key flood → IP bucket (pre-auth); XFF spoof → rightmost-trusted ignores injection; forged-key post-auth bucket → impossible (auth rejects first); nil-limiter/missing-tenant → fail-closed. R-P0-8a-F1 CLOSED, R-P2-7 CLOSED (elevated P2→Gate-A, landed).
**Gate A: 6.5 of 8. Remaining: R-P0-8b (adapter circuit breaker), R-P0-7 (consent evidence token), R-P0-6 (recon framework).** Builder cleared for R-P0-8b.

### VR-45 — 19 Jul (Fable 5): R-P0-8b circuit breaker (a508083) — money-safety CORRECT; 1 MEDIUM finding by falsification (inert threshold).
**Money/safety paths VERIFIED CORRECT at source + my runs:** open circuit short-circuits to OutcomeUnknown WITHOUT record() (adapter.go:183-184) → recovers via cooldown, never self-traps open (concern: short-circuit-recorded-as-own-failure = resolved); transport/no-response → record(false); `record(resp.StatusCode < 500)` → a 4xx business-FAILED counts HEALTHY and never trips (only 5xx/transport = breaker-failure — Appendix-D consistent, TEN-004 intent); half-open concurrency mutex-serialized (exactly one probe); open→Unknown not Failed (INV-009 money-safe, resolver enquires). Breaker unit pack + TestRP08B + EDG-005 all PASS -race; CI green.

**R-P0-8b-F1 — MEDIUM (armed-but-dead-in-EFFECT; must close before Gate A signs): `circuit_error_threshold_pct` is read but INERT.** In `record`, a success in CLOSED state zeroes BOTH failures and total, so between resets the window contains ONLY failures → at the trip check `failures == total` ALWAYS → `failures*100 >= thresholdPct*total` reduces to `100 >= thresholdPct` → TRUE for any threshold ≤ 100. The breaker therefore trips on `minRequests` CONSECUTIVE failures regardless of the configured percentage; threshold_pct=10 and threshold_pct=90 behave identically. A governed config knob that does nothing = the exact class the audits/no-hardcoding rule flag; also a semantic lie (the field name promises a rolling error-RATE; behavior is consecutive-count). **Caught by the falsification lens:** "what threshold value changes behavior? none → inert." **Test blind spot confirmed:** only thresholdPct=50 is ever tested; no test varies it, and TestBreaker_TripsAtThresholdOverMinSample would pass with any value — its name asserts a property the code lacks.
**Fix:** a true rolling window (ring buffer of last N outcomes, or don't reset `total` on success — keep a sliding sample) so threshold_pct gates as intended; OR if consecutive-failure IS the intended semantic, drop threshold_pct + rename (but TEN-004 implies rate-based, and a percentage field is already exposed → make it real). **Adversarial test required:** a mixed sequence that TRIPS at threshold=50 but NOT at threshold=90 over the same window — the test that distinguishes the knob.
Not a money-safety hole (a DOWN telco IS broken — the safety goal holds); it's a config-integrity/no-armed-but-dead defect. **Gate A: 7 of 8 with R-P0-8b-F1 open. Remaining: R-P0-8b-F1, R-P0-7 (consent evidence), R-P0-6 (recon framework).**

**Builder resolution — R-P0-8b-F1 (fold, follow-up to a508083): threshold now GOVERNS.** `breaker.record` no longer zeroes the window on success. The breaker holds a ring buffer of the last `circuit_min_requests` outcomes (`window []bool`, running `failures` count, `filled`); a success now DILUTES the rolling rate instead of erasing history. Trip check unchanged in form — `filled >= minRequests && failures*100 >= thresholdPct*filled` — but `failures` is now the count *within the rolling sample*, not "everything since the last success," so the percentage is real. trip()/reset() clear the window via `clearWindow`. Half-open path, open→Unknown short-circuit, and 4xx-healthy semantics are all untouched (money-safety unchanged).
- **The distinguishing test now EXISTS:** `TestBreaker_ThresholdGovernsTripPoint` — the SAME 6-failure/4-success (60%) window OPENS at threshold 50% but stays CLOSED at 90%. If the threshold ever goes inert again exactly one assertion fails. `TestBreaker_ResponsiveTelcoNeverTrips` strengthened from alternating-50% (which passed trivially on the bug) to a genuine sub-threshold 25% error rate.
- **min_requests bounded (it now sizes the window):** validator `circuit_min_requests` was `>= 1` unbounded → now `1..10000` (finite allocation + no perpetually-below-quorum armed-but-dead sample). New reject cases `TestRP08BF1_CircuitSampleWindowBounded` {zero, absurd}. No migration — seeded value 20 is within bounds.
- No schema change. Full gate: gofmt clean; `-race` on mno + configsvc + replay + handler all PASS in isolation (whole-suite run hit a per-package DB-contention timeout in an *unrelated* handler test — reproduced green alone at 103s); golangci-lint v2 **0 issues**; gosec **0**. Awaiting CI + reviewer re-verify at source.

### R-P0-7 (=AUD-P0-007, REG-002, PRD-002/003, EDG-028) — Builder: consent proven by channel evidence, not inferred.
The gap: confirm carried only programme/offer/token; backend hardcoded `Channel:"USSD"` and reconstructed terms from the offer — proving *which offer existed*, not that *those exact terms were rendered and accepted*.
**Fix — disclosure snapshot + acceptance evidence:**
- **Mint at menu time:** `GetOffers` now mints a `disclosure_snapshots` row 1:1 with each offer, in the same tx (append-only, RLS, server-computed `content_hash`, `expires_at`=offer expiry). Mirrors `decision_snapshots`. Offer response returns `disclosure_ref`, `disclosure_text` (exact rendered body), `total_cost_text`, template/version/locale. The disclosure is governed config (`disclosure.policy`, maker-checker) — de-hardcodes USSD.
- **Require at confirm:** `disclosure_ref` (must equal the offer's canonical snapshot id — a fabricated/foreign ref cannot resolve to this offer), `channel` (∈ `allowed_channels`), `session_id`, `accepted_at` (within snapshot validity ±2min skew). Missing any → refuse; **no advance created**. Consent record now stores the snapshot binding + channel/session/accepted_at + `content_hash`=snapshot hash + nullable `telco_evidence`.
- **Fail-closed:** no active `disclosure.policy` → `GetOffers` refuses (cannot disclose → cannot offer). Validator rejects a disclosure that omits `{{repayment}}`, empty channels/locales, default-locale-not-supported (armed-but-dead floors).
- **`confirmRequestHash` UNCHANGED** (R-P0-1 intact) — the snapshot is 1:1 with the offer, so the offer already pins it; the new evidence fields are per-attempt and deliberately out of the equivalence hash (divergence still detected at the idem gate).
**DESIGN DECISION (for the reviewer to challenge at source — see build/TELCO_INTERFACE_CONTRACT_DD06.md):** the integrity anchor is a **server-persisted, append-only, content-hashed snapshot**, NOT an HMAC-signed stateless token. Rationale: proven codebase pattern, directly queryable for regulatory/support-timeline retention (R-P2-11), DB-lookup binding ≥ HMAC for *our* verification, and **no new secret/KMS surface** (which would itself be a finding given the standing KMS concern). The genuinely telco-originated, offline-verifiable **signature** is scoped to DD-06 via the `telco_evidence` slot — exactly where the finding placed it ("some evidence originates telco-side; set responsibility+format in the telco interface contract").
- New: mig **0034** (disclosure_snapshots + consents enrichment + disclosure.policy seed), `disclosure.policy` domain+validator, deterministic renderer, `GetOffers`→`[]OfferView` (rippled ~18 callers + ~30 Confirm sites via an `acceptFor` test helper), OpenAPI (offer + confirm bodies), DD-06 contract doc.
- Tests: `origination_rp07_test.go` (snapshot minted+hashed+discloses-repayment; missing-ref; dropped-session; missing-timestamp; foreign-ref & fabricated-ref mismatch; channel-not-allowed; acceptance-outside-window; **no-policy fail-closed**; every refusal asserts zero advances) + disclosure.policy validator reject pack + rewrote the M2e consent test to assert the snapshot binding/hash/channel/session (strengthened, not weakened — the recorded evidence changed by design). Full gate: gofmt clean; `-race` from-zero (36 migs) green across origination/configsvc/handler/collections/recovery/settlement/fulfilmentresolver/ops/replay/api. **Gate A: 7 of 8 built; remaining R-P0-6 (recon framework).** Awaiting CI + reviewer verify at source.

### VR-46 — 19 Jul (Fable 5): R-P0-8b-F1 rolling-window fix (1bfb874) — VERIFIED CLEAN by falsification. R-P0-8 COMPLETE.
Ring-buffer fix re-reviewed with intent to break it:
- **Eviction count-sync (off-by-one target):** on full buffer, decrements b.failures IFF the evicted slot held a failure BEFORE overwrite, then increments for a new failure → b.failures stays exactly the count of failures in window[0:filled]. No drift.
- **No premature trip:** evaluates only `filled >= minRequests`; a single early failure (filled=1) cannot trip. Rate over the full rolling window.
- **Distinguishing test is GENUINE not tautology:** TestBreaker_ThresholdGovernsTripPoint — identical 60%-error 10-sample window OPENS at threshold 50%, stays CLOSED at 90%. If threshold ever goes inert again exactly this fails. ResponsiveTelcoNeverTrips strengthened from alternating-50% (passed on the OLD bug) to genuine 25% sub-threshold.
- **Modulo-by-zero (min_requests now sizes the window):** guarded at BOTH layers — validator rejects <1 or >10000 (reject test), newBreaker floors size at 1 defensively. Defense-in-depth.
- Money-safety UNTOUCHED (re-confirmed): open→Unknown short-circuit no-record, 4xx business-FAILED healthy, half-open mutex one-probe.
My -race runs: mno + configsvc packs PASS (incl threshold + bound-reject tests); CI green. Note on builder's whole-suite -race timeout: plausibly environmental (one PG, all packages parallel under -race, handler TestM4D_BreaksQueue stuck on PG read; green in isolation 103s) — consistent with my own package-isolation runs passing; not a code defect, but a CI-parallelism note (whole-suite -race against a single PG is contention-prone; consider -p bounding or per-package DBs at scale).
**R-P0-8 COMPLETE (8a + F1/R-P2-7 + 8b + 8b-F1), reviewer-verified. Gate A: R-P0-1/2/3/4/5/8 CLOSED (6 of 8). Remaining: R-P0-7 (consent evidence token), R-P0-6 (recon framework). Builder cleared for R-P0-7.**
